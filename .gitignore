.claude/*
# v11.9.61: mockups precisam ser commitados pra build CI ver e publicar em /preview/
# (parent eh `.claude/*` em vez de `.claude/` pra permitir negation funcionar)
!.claude/mockups/
# settings.json + hooks/ versionados: sync automatico (auto-pull/auto-push) + permissoes
# viajam pra TODAS as maquinas (casa/trabalho/etc). Ver "Workflow multi-maquina" no CLAUDE.md.
!.claude/settings.json
!.claude/hooks/
# v11.9.116: agents/ (conselheiro-fable) + docs de auditoria versionados — mesmo motivo:
# precisam viajar pra todas as maquinas, nao ficar presos numa memoria local por-device.
!.claude/agents/
!.claude/audit-2026-07.md
.validate/
node_modules/
dist/
.DS_Store
Thumbs.db
# Artefato do Firebase CLI (regras do Realtime DB, deny-all). O app usa Firestore
# via CDN e faz deploy via GitHub Pages — esse arquivo não entra no build/deploy.
database.rules.json
