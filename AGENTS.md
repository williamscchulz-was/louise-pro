# Louise Pro — Baby Tracker

## Como usar este arquivo

Este `AGENTS.md` é lido automaticamente pelo Codex no início de toda sessão dentro deste repositório. Ele descreve o projeto, a stack, os princípios não-negociáveis e o workflow de entrega. Você (Codex) deve tratar este documento como contexto permanente — não precisa que o William cole ou relembre nada daqui.

**Regra dura (v10.4.7):** *toda* alteração feita neste repo precisa ser refletida aqui. Versão atual, decisões arquitetônicas, bugs latentes descobertos, padrões novos — tudo. O objetivo é que uma nova sessão comece sem contexto nenhum e ainda assim saiba o que o William sabe. Ao commitar uma mudança funcional, atualizar este arquivo faz parte do mesmo commit (não um commit separado depois).

## Nota histórica: handoff Claude Code → Codex (v11.9.147, set/2026)

Até setembro/2026 este projeto foi mantido pelo **Claude Code** (Anthropic). O William migrou pro **Codex** e este documento foi adaptado a partir do `CLAUDE.md` original. Consequências:

- **Você (Codex) é a ferramenta oficial deste repo a partir daqui.** O CLAUDE.md fica como cópia arquivada (referência histórica) mas quem toma decisão e commita é você. Se você e o CLAUDE.md discordarem, o AGENTS.md vence.
- Muita menção histórica a "Claude Code", "Sonnet/Opus/Fable", subagentes, `Agent`/`Workflow`/`Skill` são registros HONESTOS do passado — não tente reproduzir o que não tem (ex: `Workflow` era um mecanismo Anthropic; se precisar de multiagente, adaptar pro que o Codex oferece hoje ou pedir ao William).
- A pasta `.claude/` **ainda contém coisas úteis do projeto** — `mockups/` (histórico visual, importante), `vectorize/` (ferramenta potrace pro splash) — misturadas com sujeira legada de Claude Code (`hooks/`, `agents/`, `settings.json`). Não deletar essa pasta cegamente; se for limpar, MOVA os assets primeiro pra `mockups/` e `tools/` no root.
- Hooks de sync automático git (SessionStart pull, Stop push) estão em `.codex/hooks/` — versionados, ativos em toda máquina que tiver o Codex CLI configurado pra ler `.codex/hooks.json`.
- Sub-agente conselheiro Fable foi portado pra `.codex/agents/conselheiro-fable.toml` (formato TOML do Codex), mas o Codex CLI hoje não tem o mecanismo de subagente do jeito que Claude Code tinha. Ficou como referência de protocolo — na prática, se você precisar de "conselheiro forte", escale abrindo o problema pro William num ChatGPT.

-----

## Projeto

- **App**: Baby tracker pessoal para a Louise (nascida 08/03/2026), usado por mim (William) e minha esposa
- **Localização**: Blumenau, SC, Brasil (BRT, UTC-3)
- **Repositório**: https://github.com/williamscchulz-was/louise-pro
- **Live**: https://williamscchulz-was.github.io/louise-pro/
- **Versão atual**: v11.9.149 (app) / routine-engine v2.2.4
- **Botão + Novo alimento (v11.9.149):** ajuste solicitado pelo William no formulário Comida. Botão explícito PT/EN com altura mínima de 44px limpa o nome selecionado e foca o input sincronamente no gesto (teclado iOS). `foodInputRef` declarado junto aos hooks do AddForm. O alimento continua entrando nas opções após salvar o registro, usando a persistência existente.
- **Introdução alimentar: registro Comida + Banana default (v11.9.148):** pedido urgente do William para começar a lançar sólidos antes dos 6 meses. Novo tipo aditivo `food` em `entries/`, sem migração nem coleção paralela.
  - **Schema:** `{type:"food", name, date, time, notes?, id}`. O AddForm valida `name`, normaliza espaços e limita o input a 60 caracteres. **Banana** vem selecionada em todo registro novo, como primeira opção pronta.
  - **Criar e reutilizar sem corrida entre devices:** as opções são derivadas das próprias entries `food` já sincronizadas; ao digitar e salvar um nome novo, ele reaparece como chip nas próximas aberturas (dedupe case-insensitive, Banana sempre primeiro, até 10 recentes). Não existe `config/foods` separado — isso evita mais um documento compartilhado sujeito a sobrescrita concorrente entre os dois iPhones.
  - **Persistência:** usa o mesmo `addEntry` → `FB.saveEntriesBatch` atômico/offline das demais entries, incluindo undo, edição, exclusão, repetição e backup existentes. Campo `_docId` continua nunca persistido.
  - **Ring e histórico:** `food` é evento pontual do ritmo do dia, entra em `dotEvs` pela hora, com ícone vetorial `spoon`, cor âmbar e tooltip `Comida/Food · nome · HH:MM`. O Histórico ganhou filtro dedicado Comida/Food; busca por nome já funciona pelo campo `name`.
  - **i18n:** labels, instruções, placeholders, toast flexionado (`Comida registrada`) e changelog em PT/EN. O quick-action adaptativo conhece `food` e só o promove quando o histórico real daquela hora justificar, sem alterar os priors atuais de sono/mamada/fralda.
- **Fixes de contagem duplicada e alerta prematuro (v11.9.147):** pedido do William a partir de dois prints — um gráfico de sono mostrando 14h+/dia por vários dias, e um alerta "Below recommended volume · 210ml today" às 07:06 da manhã.
  - **Bug 1 — double-tap no botão Parar gerava noite duplicada:** o `stopTimer` só tinha o guard `if(!activeTimer)return`, que barra a 2ª chamada DEPOIS que o `await FB.stopTimerAndLog` da 1ª resolve — no meio, `activeTimer` ainda é truthy. **Provado no Firestore de produção:** 2 documentos separados (`mtjxgckzlqx9e` e `mtjxggl00rub3`) com timestamps de escrita separados por **3,7 segundos**, ambos `sleep 2026-09-01 21:04 dur=176min`. Fix: `stoppingRef` (`useRef`), mesmo padrão do `savingRef` do AddForm desde v11.7.3. **Duplicata histórica apagada** (só 1 nos últimos 14 dias — os outros dias que pareciam altos batem com sum(date=D), comportamento intencional do gráfico de comportamento que soma noite anterior + sonecas do dia + começo da noite atual).
  - **Bug 2 — alerta de volume disparando com dia incompleto:** o gate `nowM >= 1080` (só avalia depois das 18h) é frágil porque protege só a GERAÇÃO nova de hint, não o cenário em que o app fica aberto atravessando a meia-noite (o hint do dia anterior fica no inbox e o `sub` é reavaliado com o "novo today" quase vazio). **Provado matematicamente**: 210ml = 110+100, as 2 mamadas da madrugada de 01/09, disparado às 07:06 quando o dia mal começou. Fix (defesa em profundidade): além do horário, `dayLooksComplete = bottlesToday.length >= 5` — se tem menos que isso, não julga contra a OMS. Validado com harness: com 2 mamadas o alerta não gera mais, mesmo forçando horário alto.
  - **Métrica do gráfico de comportamento**: confirmado que "Sleep · real + awake" soma tudo com `date=D` (noite anterior + sonecas + início da noite atual). Não é bug — é a métrica escolhida. Por isso valores de 13-14h por dia calendário são normais nesta idade, mesmo que a "noite" tenha durado 10h.
  - **Verificação ao vivo:** o único bug de código foi o double-submit; a única duplicata foi apagada; o alerta enganoso foi neutralizado.
- **Pendências das auditorias fechadas (v11.9.146):** pedido "arruma o que tá pendente" — limpa a fila de achados não-corrigidos das auditorias v11.9.143 e v11.9.145.
  - **Build não publica mais app quebrado em silêncio (2 caminhos, ambos eram `console.warn`):** (a) o inline do `styles.css` não verificava se o `.replace` casou — placeholder alterado (ex: `nonce` pra CSP) = app no ar **100% sem CSS**, e o syntax guard não pega porque só valida JS; agora testa o regex ANTES + confirma que o conteúdo entrou DEPOIS. (b) o colapso das 7 tags `<script src="js/*.js">` só avisava — regex desatualizada (já aconteceu na v11.9.56, quando `milestones.js` virou a 7ª lib) = as 7 libs em **404 em produção** com deploy verde. Ambos agora `process.exit(1)`. **Testados de verdade** quebrando o `index.html` de propósito: os 2 dispararam e o exit code é 1 (o CI falha); build normal segue em 0.
  - **Backup ganhou guarda de encolhimento:** era snapshot ÚNICO sobrescrito diariamente — qualquer perda entrava no snapshot na manhã seguinte e o app exibia "✓ Backup automático salvo" no exato momento em que a rede de segurança era destruída, sem volta. Agora `saveBackup` compara `entryCount` com o `backupMeta` anterior; se caiu, copia o snapshot atual pra **`config/backupsPrev`** antes de sobrescrever e devolve `{shrank:{before,after}}`. O App troca o toast de sucesso por um aviso âmbar dizendo quantos registros faltam. `FB.loadBackupPrev()` lê o anterior (mesmo parsing gz1/legado do `loadBackup`). Custo: 1 read + 1 write extras, 1×/dia.
  - **Invariante i18n**: `InsightCards`, `StatsPage`, `TimerBar` e `MidnightMergeCard` recebiam `lang` nos call sites mas **não tinham na assinatura** — o prop era ignorado e os componentes congelavam no idioma anterior. Assinaturas corrigidas + `lang` passado nos call sites do `MidnightMergeCard`.
  - **`GrowthPage` virou `React.memo` + prop `lang`** — era a única página-gráfico sem memo (o clone `BehaviorPage` tinha desde a v11.9.116), então re-filtrava entries e re-renderizava os 3 gráficos SVG a cada tick de 5s com a tela parada. Atenção: o componente fecha com `})` agora, não `}`.
  - **`.hit44` nos botões Voltar** de Profile/Growth/Behavior (Milestones já tinha) — eram 40px, abaixo do mínimo de 44.
  - **Marcos, achados de conteúdo:** (a) **`ageRange` finalmente renderizado** — existia nos 85 marcos e nunca aparecia; o card de próximo marco mostra "janela 4,1–7,4 meses" (convertido de semanas), que é MENOS assertivo que um mês seco e respeita melhor a regra de não dar veredito; (b) **card vira acordeão** — tocar abre a `description` no lugar, com botão "✓ Ela já faz isso" separado; antes, ler "o que observar" exigia entrar no formulário de registro com a data de hoje pré-preenchida, então consultar era quase-registrar e um toque a mais gravava marco com data errada; (c) **badges filtrados por alcançabilidade** (`reachable`: badges de consulta só quando `checkupAge<=ageMonths+1`) — 7 dos 12 não podiam mudar por 7-19 meses e ocupavam ~470px de "0/N"; o resto fica atrás de "Ver todas as 12"; (d) os 2 `useMemo` da tela **nunca memoizavam** (as deps `milestoneEntries`/`upcoming` eram arrays recriados a cada render) — agora memoizados, com `doneKeys` junto; (e) key legada não vaza mais slug cru (`brings_hands_to_mouth` → "Brings hands to mouth"); (f) `T.dim`→`T.label` em 4 pontos de texto funcional.
  - **Verificado ao vivo com dado real**: acordeão abre/fecha com `aria-expanded` correto e só então mostra descrição + botão; toggle de badges 10→12→10; janela "window 4.1–7.4 mo" renderizando; "esperado agora" corretamente ausente (marcos de 6m com a Louise em 5).
  - **✅ FIRESTORE ABERTO = DECISÃO CONSCIENTE DO WILLIAM (20/ago/2026).** Perguntado diretamente sobre fechar as rules, ele respondeu: *"ta tudo certo o app é aberto mesmo"*. **Não reportar mais como achado/bug em auditorias futuras** — é escolha de produto, não descuido. O plano de correção (Anonymous Auth + allowlist de uid, migração em 4 fases, conteúdo exato do `firestore.rules`) fica arquivado na entrada da v11.9.143 e no Artifact `1cec00e9-73f1-4e4e-9d8a-6547e4f759d6` caso ele mude de ideia; até lá, não re-propor.
  - **Consequência prática registrada (não é alarme, é operação):** como leitura E escrita estão abertas, o `config/backups` está no mesmo banco que ele protege — uma deleção em massa levaria junto a rede de segurança. A única cópia fora desse alcance é o **"Baixar cópia local"** do BackupSection. Se um dia entrar rotina de disaster recovery neste projeto, é por aí (download periódico), não por rules.
- **⚠️ FIX CRÍTICO: marcos não podem ter janela de 90 dias + céu vira linha do tempo (v11.9.145):** auditoria da tela de Marcos pedida pelo William a partir de um print ("auditar informações, funcionamento, usabilidade + mockar ideias"). Workflow de 6 agentes (3 lentes: informação/funcionamento/usabilidade, com Opus nas 2 primeiras, + 1 verificador adversarial cada) → **23 achados, 0 rejeitados**. Relatório + mockups no Artifact `0d2febfb-4075-4a3a-8d71-f403e802bd16`.
  - **⚠️⚠️ O ACHADO (crítico, perda de memória):** `FB.subEntries` corta em 90 dias — certo pra mamadeira/fralda, CATASTRÓFICO pra marco. Medido no Firestore real: **23 dos 46 marcos estavam invisíveis**, incluindo `born`, `first_feed`, `first_bath`, `discharge`, `first_night_home`, `social_smile`. Cascata em 3 níveis: (1) as memórias sumiam; (2) como `doneKeys` não continha o registro antigo, o picker OFERECIA o marco de novo — **5 das 6 duplicatas reais nasceram assim** (padrão confirmado: uma cópia fora da janela, outra dentro), e o 1º sorriso ficou com data errada (real 17/04, regravado 21/07); (3) os badges se DESFAZIAM sozinhos conforme os marcos envelheciam pra fora da janela (`10 LOGGED` voltava a bloqueado) — achado do agente, eu não tinha visto.
  - **Fix**: `FB.subMilestones(cb)` (index.html) — subscription própria com `where('type','==','milestone')` e **sem corte de data**. Sem `orderBy` de propósito (where+orderBy em campos diferentes exigiria índice composto; ordena no cliente, que é barato em dezenas de docs). No App: state `milestones` + `allMilestones` (useMemo, ACIMA do early-return de splash) que **une** a subscription com os marcos que já vêm em `entries` e deduplica por `_docId` — sem isso o contador do header, o céu e o card da Home divergiriam entre si. Dedupe por `key` NÃO é feito: duplicata de key é dado real que precisa aparecer pro usuário poder apagar. A subscription fica FORA do gate de `ready` (se falhar, o app não pode travar no loading). `deleteEntry` passou a procurar o snapshot de undo também em `milestones` (marco antigo não está em `entries` → o "Desfazer" não restaurava nada, em silêncio). `MilestonesPage` recebe prop `milestones` em vez de `entries`.
  - **Céu reescrito (era rabisco por construção):** a versão v11.9.129 posicionava por `hash(key)` (aleatório no espaço) mas ligava vizinhos no TEMPO — as linhas conectavam pontos sem relação espacial nenhuma. Medido no caso real: **20 linhas, 36 cruzamentos**, e piorava com o uso (registrar vários marcos numa sentada gera o máximo de linhas, todas com gap 0). A anti-colisão gulosa desistia em 6 de 26 estrelas, sobrepondo pares a 10,7px. **Agora a POSIÇÃO carrega o tempo**: X = ordem cronológica, com **serpentina** (boustrofédon) de até 4 faixas — uma faixa só não escala (38 marcos dariam 2,3 unidades de espaçamento; o catálogo tem 85). Verificado ao vivo com os 38 marcos reais: **0 cruzamentos** (era 36), trilha contínua de 37 segmentos, menor distância entre estrelas 2,5→**6,7**. `skyH` passou a acompanhar o nº de FAIXAS, não a contagem crua (que saturava o teto de 300px já aos 18 marcos). Cor da estrela = categoria do marco (4 cores confirmadas no DOM) — informação que a tela já tinha e desperdiçava em 21 estrelas douradas idênticas. Estrelas apagadas ganharam `onClick` e `aria-label` (eram `pointerEvents:none` — o usuário via algo apagado sem como descobrir o quê).
  - **Rótulo enganoso corrigido:** `isExpected` era `m.checkupAge<=ageMonths+1`, e como o filtro de `upcoming` já corta em `nextCheckup`, isso dava **sempre true** — 100% dos cards diziam "esperado agora", inclusive marcos de 1 mês à frente. Efeito real: o pai lê "esperado agora" pra algo que a bebê não faz e conclui atraso, quando falta um mês. Agora `<=ageMonths`, e o resto diz "na consulta de Xm".
  - **Informação que existia e nunca era mostrada:** card de próxima consulta no topo (`nextCheckup` era calculado e só usado pra filtrar) com semanas restantes + "N de M marcos registrados"; e o card "Primeiros momentos" quando faltam marcos de recém-nascida (o `Math.max(2,...)` do filtro de `upcoming` os excluía de TODA idade, e o badge cobrava "0/10" sem oferecer caminho). Header: contagem passou a ser de marcos ÚNICOS e "5m"→"5 meses" ("m" é minuto no resto do app).
  - **Validação ao vivo com dado de produção**: header 21→**38** marcos, "38 STARS LIT", badge **NEWBORN saiu de "0/10" e foi CONQUISTADO** (os marcos existiam o tempo todo), conquistas 3/12→4/12, card de consulta mostrando "in ~3 weeks · 7 of 11".
  - **Ainda em aberto desta auditoria** (documentado no Artifact): descrição do marco só legível dentro do fluxo de registro (consultar = quase-registrar); `ageRange` (janela de aquisição) existe nos 85 marcos e nunca é renderizado; 7 dos 12 badges são inalcançáveis por 7-19 meses e ocupam ~470px; os 2 `useMemo` da tela não memoizam (deps recriadas a cada render); marco com key legada vaza o slug cru na timeline.
- **Evento do dia (tags de contexto) + higiene de médias (v11.9.144):** pedido a partir de uma lista de sugestões externas (Flávia Vergara) que o William repassou pedindo "veja o que faz sentido" — ele escolheu as 5 tags: **VACINA, DENTE, DOENÇA, ALIMENTO, CO-SLEEPING** (implementado como "alimento NOVO", ver abaixo). Motivação: sem contexto, as Comparações do relatório continuam confundindo coincidência com causa — uma noite ruim depois da vacina não é sobre leite.
  - **Modelo de dado**: tipo de entry novo `dayevent` com `{tag, untilDate?}`. `EVENT_TAGS` (`00-core.jsx`) define cada tag com `effect` (janela de dias) e `spans` (se aceita período). **Vacina `effect:3`** e **alimento novo `effect:2`** marcam sozinhos os dias seguintes — a reação raramente cabe no dia do registro. **Dente e doença `spans:true`** aceitam `untilDate` (o pai marca até quando durou em vez de remarcar toda manhã — atrito baixo o suficiente pra ser usado às 3h). Co-sleeping é só a noite. `_eventDaysByDate(entries)` devolve `{data → [tags]}` já expandido pela janela; `sleepReport` põe isso em `row.tags`.
  - **Decisão sobre "ALIMENTO"**: implementado como **"Alimento novo"/"New food"**, não "alimento" genérico. Motivo: a Louise faz 6 meses em 08/09/2026 (~3 semanas depois desta versão) e começa introdução alimentar — "alimento" genérico viraria ruído diário, "alimento novo" é o dado que explica reação/sono.
  - **`sleepCorrelations(rep,{cleanOnly:true})`**: roda a MESMA comparação só sobre dias sem tag. A UI mostra as duas versões (o ganho principal das tags não é explicar, é **limpar**). Só renderiza a versão limpa quando os 2 grupos ainda têm N>=MIN_N — senão seria número frágil disfarçado de rigor.
  - **Higiene de médias (achado da própria sugestão, confirmado no código):** os KPIs do relatório incluíam **hoje** (dia incompleto — às 14h a Louise fez 2 das 4 sonecas), contaminando tudo. Só as Comparações da v11.9.142 excluíam. Agora `row.partial` marca hoje, os KPIs usam `done=rows.filter(!partial)`, a linha aparece marcada "PARCIAL" (cinza na tabela e no canvas), e as médias móveis também excluem o dia parcial da janela.
  - **1ª soneca da manhã**: `firstNapMin`/`firstNapStart` (o dado já existia em `dayNaps[0]`, só não era exposto) + `firstNapMA7`. É o indicador de consolidação de ritmo. Coluna nova na tabela e no PDF, e média no KPI de sonecas.
  - **Canvas/PDF**: tags viram **sigla de 3 letras** (VAC/DEN/DOE/ALI/C-S), não emoji — emoji em canvas 2D depende de fonte do sistema e imprime mal em P&B, e este documento vai impresso pra pediatra. Legenda das siglas no rodapé, só quando há tag no período. `W` 830→**920** (medido: as 17 colunas + gaps + padding precisam de 902px; ~18px de folga pra 3 tags no mesmo dia).
  - **Verificação**: harness de 27 asserts extraindo `EVENT_TAGS`/`_eventDaysByDate` do **fonte real** (não reimplementação) — janela da vacina, período do dente, sobreposição de 2 tags no mesmo dia, tag desconhecida ignorada, duplicata. Ao vivo com dado de produção: tag sintética de dente 10→12/08 marcou exatamente 3 dias, KPI "só dias normais (26)" = 29 noites − 3 marcadas, linha PARCIAL = hoje, 0 estouros de largura em 510 células do canvas, e PDF de 685KB com os 5 offsets do xref batendo com as posições reais. Doc de teste apagado depois.
  - **⚠️ Landmine de ambiente (nova):** o servidor estático de verificação roda **de dentro de `dist/`**, e no Windows isso trava o diretório — o `build.mjs` passa a falhar com `EPERM` ao tentar recriar `dist/`. Parar o servidor antes de rebuildar (ou servir de fora com `--directory dist`).
  - **Sugestões avaliadas e NÃO implementadas, com motivo** (registrado pra não serem re-propostas sem contexto novo): (1) **comparativos pareados** (dia de 3 sonecas vs o dia seguinte de 4) — estatisticamente corretos e atacam o viés que o próprio relatório confessa, mas o Opus/Fable já mediram que com 30 dias nenhuma comparação sai do ruído e 4 dos 7 números mudam de sinal com 1 dia a menos; parear derruba o N ainda mais. Revisitar com 90+ dias. (2) **Alerta de mudança de fase** (MA7 subir X% sobre baseline de 14d) — hoje apitaria por ruído; só faz sentido depois das tags e com limiar conservador. (3) **Semáforo de "prontidão pra transição de sonecas"** — **rejeitado por natureza, não por esforço**: vira conselho clínico num app que se declara "documento de acompanhamento, não substitui avaliação profissional", e um verde errado faz cortar uma soneca que a bebê ainda precisa. Mostrar os sinais crus sim, o veredito não. (4) **Colunas de introdução alimentar** — iminente (6 meses em 08/09/2026), mas é feature própria: precisa de tipo de entry, UI de registro e provavelmente aceitação/reação por refeição.
- **⚠️ AUDITORIA COMPLETA multiagente + 3 fixes de perda de dado (v11.9.143):** rodada de auditoria pedida pelo William ("auditoria completa no app, usabilidade design e tudo mais, infra estrutura, segurança") com orquestração explícita de agentes. **Workflow de 13 agentes**: Haiku no inventário mecânico, **Opus** em segurança e integridade de dados (as 2 dimensões de risco alto), Sonnet em usabilidade/design/infra/perf-a11y, + 1 verificador adversarial por dimensão instruído a REJEITAR na dúvida. 28 achados levantados → **27 confirmados, 1 rejeitado** na verificação. Relatório completo + mockups publicado como Artifact (`1cec00e9-73f1-4e4e-9d8a-6547e4f759d6`).
  - **⚠️⚠️ ACHADO CRÍTICO EM ABERTO — FIRESTORE SEM NENHUMA REGRA:** qualquer pessoa lê **e escreve** tudo sem auth (confirmado empiricamente por `curl` sem token: perfil, entries, backup, tokens FCM). A apiKey está no HTML servido publicamente, então **privar o repo NÃO resolve** — só as rules resolvem. **Correção desenhada pelo Opus e NÃO aplicada ainda** (exige passos no console do Firebase que só o William pode fazer): **Anonymous Auth + allowlist de uid**. As 3 alternativas foram avaliadas e reprovadas com justificativa — App Check não é controle de acesso (as rules seguiriam abertas, e o token é minerável num browser headless); chave secreta em campo não protege LEITURA; restrição de apiKey por referrer não se aplica à API do Firestore e referrer é forjável. Conteúdo exato do `firestore.rules` + migração em 4 fases está no Artifact. **FASE 0 BLOQUEANTE**: confirmar que o worker de push usa Admin SDK e não a apiKey pública — há evidência de worker escrevendo em `config/reminders/items/` de fora do app; se ele usar o SDK cliente, QUEBRA quando as rules fecharem. Nota: os tokens FCM já vazados continuam válidos depois de fechar as rules (~270d até rotação natural) — apagar `config/pushTokens/tokens/` pro app re-registrar.
  - **VAZAMENTO DE PRIVACIDADE FECHADO (aplicado):** `build.mjs` copiava `.claude/mockups/` INTEIRO pra `dist/preview/` (feature da v11.9.61) e o `relatorio-30d.html` — gerado com dados REAIS — estava público com HTTP 200 (nome completo, data de nascimento, 30 dias de rotina), indexável. A cópia foi **removida do build**, não filtrada: a v11.9.67 já documentava que `/preview/` nunca funcionou como superfície de revisão pra ele, então era risco permanente com zero utilidade. Confirmado 404 em produção depois do deploy. Varredura dos 34 mockups versionados por contagem de datas/horários distintos isolou só 2 com dado real (`relatorio-30d` removido, `settings-redesign` sanitizado) — o resto era exemplo de UI legítimo. Regra nova registrada na seção de workflow.
  - **FIX CRÍTICO 1 — editar metade de noite partida apagava a outra metade:** `addEntry` rodava `splitMidnight` em TODO save, inclusive edição, e `saveEntriesBatch` usa `set()` sem merge — então editar a metade `X` RECONSTRUÍA e sobrescrevia o doc `X_b` real, que tem dados próprios (os `wakings` gravados ao vivo de madrugada). Editar "21:00→00:00 · 3h" destruía 7h de sono e o despertar das 03:15, em silêncio. Ironia: o `MidnightMergeCard` da v11.9.140 é quem convida o gesto (botão de editar em cada metade). Fix: `pairExists` (edição + par já existe) → grava só o doc editado, sem split. Criação normal segue partindo.
  - **FIX CRÍTICO 2 — "Desfazer" pós-edição APAGAVA o registro:** o undo do toast era sempre `deleteEntriesBatch`, sem olhar `wasEdit`, enquanto o texto dizia "atualizada". Corrigir 120→150ml e tocar em Desfazer apagava a mamadeira — e essa exclusão não tinha undo próprio. Fix: `beforeEdit` (snapshot da entry original capturado antes da mutação) + undo ramificado — edição restaura, criação apaga, e quando não dá pra restaurar com segurança o undo vira `null` (toast sem botão é melhor que botão que destrói).
  - **FIX CRÍTICO 3 — Ajustes revertia a mudança do outro iPhone:** `isDirty` comparava os campos locais contra o prop `profile` VIVO. Com Ajustes aberto num device e alguém mudando no outro, o snapshot chegava, o local continuava velho, `isDirty` virava true SOZINHO e o autosave de 900ms (sem botão Salvar desde a v11.7.2) gravava o valor velho por cima. Mesmo mecanismo revertia `photo`/`name` — o par que sumiu na v10.4.7. Fix em 2 metades: (a) `baseRef` congelado no mount, `isDirty` compara contra ele (mudança remota não dispara dirty); (b) **re-sync por campo** — campo não-sujo adota o valor remoto e avança o baseline, campo sujo mantém o local (senão a tela ignoraria pra sempre o que o outro device mudou). `commitBaseline()` avança após save; `revertAll` também alinha o baseline.
  - **Verificação:** 2 harnesses (16 + 17 asserts) reproduzindo cada bug ANTES e provando o fix DEPOIS, incluindo os casos de não-regressão (criação ainda parte na meia-noite; undo de criação ainda apaga; edição normal de 1 device só continua funcionando; 2 devices editando campos diferentes preservam ambos). Smoke test ao vivo: Ajustes aberto 5s sem toque → **0 mutações de DOM, autosave não disparou, sem loop de render**; perfil de produção conferido intacto depois (`mlGoal` 1000, nome, bedtime 19:30).
  - **Achados NÃO corrigidos nesta rodada** (documentados no Artifact, priorizados pelo William pra depois): build publica app quebrado em silêncio em 2 caminhos (CSS não inlinado e bundle não colapsado → só `console.warn`, deveria ser `exit(1)`); backup diário sobrescreve o único snapshot (qualquer perda vira permanente em <24h — precisa de guarda de encolhimento + `backupsPrev`); editar/excluir despertar trava o cartão pra sempre offline (sem spinner/timeout); React vem do unpkg em versão flutuante sem SRI; 4 componentes `React.memo` sem prop `lang`; `GrowthPage` sem memo; `T.dim` em texto funcional (2.3:1, abaixo do WCAG AA); botão Voltar sem `hit44` em 3 das 4 telas full-screen.
- **Relatório: seção "Comparações" (4 correlações) — passou por escalação real ao Opus+Fable (v11.9.142):** pedido do William em cima da v11.9.141 ("as correlações, mas não pode ter erro estatístico, qualquer coisa pede ajuda pro Fable") — as 4 que tinham ficado de fora da rodada anterior por serem "feature nova de verdade" (sonecas 3 vs 4 → noite seguinte; janela pré-bed >2h30 vs <2h30 → despertares; leite do dia vs despertares; bedtime antes/depois de 19h30 → duração). **Primeira vez neste projeto que a escalação Sonnet→Opus documentada no CLAUDE.md foi usada de verdade** (não só como protocolo escrito) — Opus revisou e, por julgamento próprio, também acionou o Fable (cadeia linear normal, sem eu chamar nenhum dos dois direto).
  - **O que a revisão achou:** minha 1ª versão tinha a DIREÇÃO TEMPORAL certa (sonecas/leite do DIA comparados com a noite que COMEÇA aquela noite — `nightStartingHere`, não a noite que já tinha acabado de manhã — inverter isso compararia efeito com causa que ainda nem tinha acontecido) mas um **bug real**: a mediana do leite incluía o dia de HOJE (incompleto, ainda rolando) — sozinho, isso produzia o resultado "mais leite = mais despertar" que eu mesmo já tinha achado suspeito ao ver o dado real. Além disso, um teste de significância (Welch, todas as 8 combinações comparação×métrica) mostrou que NENHUMA das 4 comparações, com só 30 dias de dado, é distinguível de ruído — e leave-one-day-out mostrou que 4 dos 7 números mudam de SINAL se um único dia sai da amostra. 3 das 4 variáveis (sonecas, leite, bedtime) são fortemente confundidas com a TENDÊNCIA DENTRO DO PRÓPRIO MÊS (a Louise consolidando rotina ao longo das mesmas semanas medidas) — comparar "3 sonecas vs 4" é em boa parte comparar "agosto vs julho". E a comparação de bedtime original usava DURAÇÃO como métrica, que é quase uma identidade aritmética com o próprio bedtime (duração≈acordar−bedtime) — ia sempre "achar" diferença artificial.
  - **Fix aplicado** (`sleepCorrelations` em `00-core.jsx`, reescrita completa validada por 11/11 asserts contra dado real de produção antes de entrar no código): hoje é excluído de todo grupo (dia incompleto); `MIN_N` subiu de 5→7 (comunicável como "menos de 1 semana no grupo"); em vez de correlação/p-valor (falsa precisão pra leigo, e precisaria de correção de múltiplas comparações rodando as 4 juntas), cada comparação ganha um selo binário "dentro do normal" / "vale observar" via `|diferença das médias| >= desvio-padrão do pool dos 2 grupos` (equivalente a Cohen's d>=1 — ideia do Fable, estritamente melhor que a proposta inicial do Opus porque é ao mesmo tempo mais conservadora — absorve a multiplicidade de graça nesses tamanhos de grupo — e mais explicável); `dayGap` (distância média em dias entre os 2 grupos dentro do mês) é EXPOSTO, não corrigido — se os grupos estão a 5+ dias de distância, a UI mostra uma nota explícita de que pode ser tendência do mês, não a variável; bedtime passou a comparar HORÁRIO DE ACORDAR (`wake`), não duração (mata a identidade aritmética), com guarda extra pra noite que termina antes da meia-noite (`endAbs>=1440`) não poluir a média de horário. **Verificado ao vivo com dado real pós-fix**: mediana do leite corrigiu de 890→880ml, grupos rebalanceados (14/15), grupo pequeno da janela pré-bed (n=6) corretamente suprimido ("dado insuficiente"), bedtime mostrando gap de ~20min no horário de acordar (não mais os 58min inflados de duração), e as 4 comparações saíram todas como "dentro do normal" — a resposta honesta pra este mês de dado.
  - **Linguagem**: seção renomeada "Correlações"→"**Comparações**" ("Correlations"→"**Comparisons**") — o que é mostrado é diferença descritiva de médias entre 2 grupos, não correlação estatística de verdade. Disclaimer reescrito nomeando o confundimento por tendência explicitamente. Nunca frase causal ("X causa Y") — só "nas noites que vieram depois de X, a média foi Y".
  - **Nota registrada pelo Opus pra decisões futuras**: (1) baixar o piso do selo "vale observar" abaixo de Cohen's d≈1 exige repensar correção de multiplicidade (hoje ele absorve isso "de graça" pelo tamanho pequeno dos grupos — não é garantido continuar assim se os grupos crescerem); (2) as Comparações deliberadamente INCLUEM noites marcadas `nightOutlier` (que o KPI de sono noturno exclui da média "sem outliers") — decisão consciente: filtrar seria mais um grau de liberdade de pesquisador escondido, e o desvio-padrão do pool já cresce quando tem outlier no grupo, o que naturalmente torna o selo "vale observar" mais difícil de disparar (o selo se autoprotege).
  - **Escopo**: só na tela (`57-report.jsx`), não no PDF/canvas — mesma decisão de risco da tendência de 7d na v11.9.141 (Y-offsets em cascata no canvas hand-rolled, sem visual pra verificar cegamente).
  - **Verificação pós-fix**: `node build/build.mjs` limpo (achei e corrigi eu mesmo um `</div>` órfão que sobrou de um `old_string` de Edit que não capturou o fechamento inteiro do componente `Corr` antigo — pego pelo próprio syntax guard do build, não foi silencioso). Testado ao vivo com dado real de produção (mesma metodologia sem screenshot da v11.9.141 — leitura de texto/DOM via `javascript_tool`).
  - **Entrega intermediária**: o William pediu no meio do trabalho ("parece que não mudou nada") pra ver o relatório de verdade — em vez de mockup desenhado, capturei o **HTML real renderizado** (`document.getElementById('lp-report').outerHTML`) da tela rodando com dado de produção, depois do fix, e publiquei como Artifact (link HTTPS de verdade, abre no iPhone sem o problema de localhost documentado na v11.9.67). Arquivo local em `.claude/mockups/report-snapshot-v141-142.html`.
  - **Ajuste rápido pós-review**: card de sonecas ganhou **duração média por soneca** por grupo (`avgNapMin`, achata `napDurations` dos dias do grupo e tira média simples) — descritivo a mais dentro de uma comparação já revisada, não é correlação/comparação nova, sem risco estatístico adicional; só aparece no card de sonecas (prop `showNapAvg` no `Corr`), não nos outros 3.
- **Relatório de 30 dias: outliers, detalhe de sonecas/despertares, janela pré-sono, tendência (v11.9.141):** pedido a partir de feedback concreto sobre o relatório real (mensagem trazendo ideias organizadas em "quase de graça" / "esforço moderado" / "feature nova" — William pediu pra fazer as 2 primeiras categorias agora; a 3ª — anotação de evento do dia tipo vacina/dente/febre e as 4 correlações cruzadas — ficou de fora, não pedida ainda). Tudo em `00-core.jsx` (motor, `sleepReport`/`_rpMergeSessions`) + `57-report.jsx` (tabela HTML on-screen E o canvas/PDF exportado — as DUAS superfícies de render, deliberado: o PDF é o que realmente vai pra pediatra, deixar só a tela bonita e o PDF desatualizado seria pior que não fazer):
  - **Outliers de sono noturno**: critério literal do pedido do William — noite `<6h` OU que começou `>=23h` vira `nightOutlier` (novo campo por linha em `sleepReport`). O KPI "Sono noturno" mostra a média de sempre + uma linha extra "Excl. N outliers: Xh Ym" SÓ quando existe outlier de verdade (não polui quando não tem). Na tabela, a noite atípica ganha um `*` amarelo do lado da duração (HTML) / a linha inteira fica com fundo âmbar (`#fbf1de`, canvas — mais simples de desenhar em canvas que misturar 2 cores numa mesma `fillText`, mesma informação por meio diferente). **Verificado com dado real**: pegou exatamente a noite que o William citou como exemplo (5h50m, 01:40→07:30) — confirma que a noite dele não bateu com o que eu tinha reconstruído mentalmente da sessão anterior (era uma sessão de sono CURTA e SEPARADA, não a metade de uma noite partida por `splitMidnight`) e que o critério literal funciona no caso real que motivou o pedido.
  - **Sono total do dia**: `totalSleepMin` (noite+sonecas) já existia no motor desde a v11.9.135, só não estava exposto na tabela — coluna nova "Day total"/"Sono total", zero cálculo novo.
  - **Sonecas e despertares individuais**: `dayNaps[]` já tinha `{start,end,min}` por soneca (só não renderizado individualmente); `_rpMergeSessions` ganhou `wakingsDetail` (tempo+duração de cada despertar — campo NOVO ao lado de `wakings`, que continua sendo só a contagem, pra não quebrar quem já lê isso). Tabela ganhou 2 colunas de lista compacta ("Naps (min)"/"Wak. (min)", ex: "45,50,40") — horário de cada soneca não entrou na tabela por decisão consciente (já dá pra ler no mapa/actograma acima, que é posicional; duplicar como texto só ia poluir).
  - **Janela pré-sono**: `windows[]` já existia (todas as janelas acordada do dia); campo novo `preBedWindowMin` acha a janela cujo fim bate exatamente com o horário do bedtime (`w.to===bed.start`, match direto — não assume que é sempre a última do array, que seria uma suposição frágil se algum evento acontecer depois do bedtime no mesmo dia). Fica como coluna NOVA ao lado da "Window" (maior janela) que já existia — não substituiu, porque o KPI do topo ("Janela acordada... maior do dia") ainda se refere à maior, mudar o significado dali quebraria essa referência cruzada.
  - **Tendência de despertares (média móvel 7d)**: `wakingsMA7`/`realSleepMA7` novos por linha (janela de 7 dias à esquerda incluindo o próprio dia, mínimo 2 noites rastreadas na janela — mesmo piso de dado mínimo que `behaviorWeekly` já usa noutra tela). Vira 1 linha de texto no topo da tela ("tendência 7d: X/noite ↑/↓ vs Y há 7 dias", comparando `rows[último].wakingsMA7` com `rows[último-7].wakingsMA7`) — **decisão consciente: só na tela HTML, não no PDF/canvas**. Motivo: mexer nisso no canvas exigiria empurrar TODOS os Y-offsets subsequentes pra baixo (mapa, tabela, rodapé — tudo hardcoded em cascata a partir de `mapY=224`), um tipo de mudança que não dá pra verificar visualmente neste ambiente (ver nota de verificação abaixo) e que arriscaria muito mais que o ganho justifica pra uma métrica que é mais valiosa no uso interativo dos pais do que num PDF estático pra pediatra.
  - **⚠️ Refactor de robustez no canvas**: as 15 colunas da tabela do canvas (`renderReportCanvas`) pararam de usar `x` hardcoded coluna-por-coluna (o jeito antigo, difícil de ajustar sem recalcular tudo a mão) — agora é um array de `{largura,alinhamento}` (`colDefs`) com `x` DERIVADO por acumulação (`cx+=largura+GAP`). Motivo: adicionar 4 colunas à mão nas posições antigas seria exatamente o tipo de mudança frágil que quebra silenciosamente (texto cortado/sobreposto) sem dar erro de build. Canvas também alargou de `W=780`→`830` pra caber as colunas novas sem espremer.
  - **Verificação sem visual/screenshot (Browser pane sem compositing de novo nesta sessão)**: a tabela HTML foi checada por `document.body.innerText`/`querySelector` reais (dado de produção, 905 entries, 30 linhas) — headers, valores de linha, KPI com outlier, nota de tendência, tudo conferido em texto. O canvas/PDF (que NÃO é inspecionável assim, é pixel puro) foi verificado por um método diferente e mais rigoroso que "olhar print": (1) chamei a função real `renderReportCanvas` com o `rep` de produção de verdade e repliquei o cálculo de `colDefs`/`ctx.measureText` num script à parte, comparando a largura RENDERIZADA de cada uma das 450 células reais (30 linhas × 15 colunas) contra o orçamento de cada coluna — zero overflow; (2) usei `ctx.getImageData` pra ler o PIXEL de volta do canvas na posição exata da linha do outlier real e confirmei RGB `(251,241,222)` = `#fbf1de` batendo exato, contra `(255,255,255)` nas linhas vizinhas; (3) rodei `renderReportPDF` de ponta a ponta com dado real e reapliquei a MESMA validação de offsets de xref que o harness original desta função já fazia (achar cada `N 0 obj` de verdade no binário e comparar com a tabela xref) — bateu, `%PDF-1.4`...`%%EOF`, ~635KB. Znenhuma dessas 3 checagens precisou de screenshot — é um padrão replicável pra próxima vez que o Browser pane não compositar.
  - **Escopo consciente, não implementado agora**: as 4 correlações cruzadas (sonecas×noite, janela-pré-bed×despertares, leite×despertares, bedtime×duração) e o campo de anotação de evento do dia (vacina/dente/febre) — o William classificou como "feature nova de verdade" no próprio pedido e não entraram no "agora". CSV/texto compartilhável (`sleepReportCSV`/`sleepReportText`) também não ganharam os campos novos — o pedido era especificamente sobre "a tabela".
- **5 achados de usabilidade corrigidos + fix do bedtime confirmado ao vivo (v11.9.140):** rodada baseada numa auditoria de usabilidade (achados mockados em `.claude/mockups/audit-fixes.html`, publicado como Artifact, aprovados em bloco pelo William — "pode aplicar todas... e corrigi bedtime claro", esse último pedido interpretado como fechar a pendência de verificação visual do fix da v11.9.139, já que o texto dela mesma listava isso como pendente). Implementados os 5 achados com componente visual (os outros 4 da auditoria — falha de escrita silenciosa, debounce do Ajustes, texto do restore, erro do relatório — eram só lógica/texto, sem mock, ficam pra outra rodada):
  - **Achado #1 (alta) — noite/soneca partida na virada da meia-noite virava 2 registros soltos:** `splitMidnight` (`00-core.jsx`) sempre gerou 2 docs Firestore com IDs previsíveis (`part2.id = part1.id+"_b"`), mas a UI nunca usou esse vínculo — editar ou apagar uma metade não avisava sobre a outra. Novo helper puro `findMidnightPair(entry,entries)` acha o par pelo contrato de ID (mais direto que remontar por hora/data como `_rpMergeSessions`, que existe pra AGREGAÇÃO do relatório, não pra lookup de uma entry específica) — só considera merge quando NENHUMA metade tem `wakings` (noite com despertar já vira `SleepBlock` normalmente; escopo consciente, não mexido). Novo componente `MidnightMergeCard` (`30-sleepblock.jsx`) mostra as 2 metades como 1 cartão só (duração total, badge "began yesterday"/"começou ontem" na paleta do `nightBadge` já existente); expandir revela cada metade com seu próprio editar/excluir — REUSA `onEdit`/`onDelete` já existentes por entry, **sem** inventar um payload novo de "editar a noite inteira como uma coisa só" (decisão consciente: isso arriscaria reintroduzir a MESMA classe de bug da v11.9.139). Fica em: History (`52-history.jsx` — metade "de início" some do dia em que começou, reaparece mesclada no dia em que terminou, mesma convenção do relatório de 30 dias) e Home (`90-app.jsx` — só precisa checar a metade `_b`, já que a metade "de início" nunca cai em `todayE`, sempre é de ontem). Busca no Histórico continua mostrando as 2 metades soltas (escopo cortado deliberadamente — resultado de busca é orientado a match, não à narrativa da noite). **Verificado ao vivo com dado real**: noite 19:40→07:30 (10h10m) apareceu mesclada em Home E History simultaneamente.
  - **Achado #3 (média) — apagar a noite inteira no `SleepBlock` era 1 toque sem confirmar:** ganhou o mesmo confirm de 2 toques ("Sure?"/"Certo?", 3s de auto-cancelar) já usado nos outros 2 botões de excluir do mesmo componente. **Verificado ao vivo**: 1º toque muda label+cor+background corretamente, 2º toque (não executado no teste) apagaria, 3s sem confirmar reverte sozinho — confirmado o ciclo completo sem apagar nada de verdade.
  - **Achado #4 (média) — editar/remover despertar não dava feedback nem desfazer:** `SleepBlock` ganhou prop `showToast` (propagado de App → HistoryPage → SleepBlock e App → SleepBlock direto no Home/liveBedtime) — remover ou editar o horário de um despertar agora mostram toast com **desfazer** (restaura o array de wakings anterior, capturado antes da mutação). Exceção consciente: encerrar um despertar AO VIVO (isActive→closed) mostra toast mas SEM desfazer — reabrir um waking ao vivo reconstruindo o `nightWake` original é frágil (perderia o tempo real decorrido); não vale o risco pra um caso raro. **Verificado ao vivo COM CICLO COMPLETO em dado real** (ver landmine abaixo).
  - **Achado #5 (média) — formulário de edição não tinha excluir:** `AddForm` ganhou prop `onDelete` (só passado quando `editEntry` existe) + botão de lixeira ao lado do Salvar, com o MESMO confirm de 2 toques já usado no delete de remédio dentro do próprio AddForm (`confirmDelMed`) — **não** copiado do `GrowthPage.doDelete`, que ainda usa `confirm()` nativo (achado incidental durante a leitura: GrowthPage ficou pra trás do padrão migrado na v11.9.35/38 pro resto do app; não mexido agora, fora do escopo desta rodada — próxima vez que mexer em GrowthPage, migrar). Wiring em `90-app.jsx`: `onDelete` chama `deleteEntry` (toast+undo de sempre) e fecha o form. **Verificado ao vivo**: botão aparece em modo edição com o estilo "não confirmado" correto; ciclo de confirmação não foi levado até o fim (ver landmine abaixo — depois do susto do achado #4, preferi não arriscar 2ª vez).
  - **Achado #9 (média) — os 3 botões rápidos podiam trocar de tipo no mesmo slot:** `suggestQuickActions` (`00-core.jsx`) fixa Mamadeira e Fralda nos slots 1-2 SEMPRE — só o 3º slot continua escolhido pelo score adaptativo de sempre. **Bug pego SÓ testando ao vivo** (não no code review, nem no `build.mjs`): a 1ª versão fixava QUAIS 3 tipos apareciam mas ainda reordenava o array final pelo `SLOT_ORDER` antigo (pensado pra agrupar por CATEGORIA — alimentação/sono/cuidado) — resultado: "Fralda" pulava entre a posição 2 e a 3 dependendo de qual era o tipo adaptativo (`nap`/`sleep` empurravam fralda pra direita; `bath`/`medicine` deixavam ela no meio). Era a MESMA classe de instabilidade que o achado #9 queria matar, só que num campo diferente. Fix: o array já sai pronto na ORDEM VISUAL final (`["bottle","diaper",adaptive]` ou `["wakeup","bottle","diaper"]`) — SLOT_ORDER virou código morto e foi removido. **Trade-off consciente**: Amamentação perde a chance de ocupar os slots 1-2 (só disputa o 3º agora); não é problema pra Louise (mamadeira E amamentação em uso ativo, `NursingSidePicker` + `hourlyTypicalMl` já provam isso), mas registrar caso um dia o padrão de uso mude pra amamentação exclusiva. **Verificado ao vivo com dado real**: confirmado "Add Bottle"/"Add Diaper"/"Add Nap" nessa ordem exata.
  - **⚠️ LANDMINE descoberta ao vivo: toast de desfazer pode expirar ANTES do round-trip de uma ferramenta de automação conseguir clicar nele.** Testando o achado #4 numa entry REAL (não sintética), completei o ciclo de remover um despertar — o toast "Waking removed" apareceu certinho — mas o tempo entre chamadas separadas da ferramenta de browser (cada uma com latência própria de ida-e-volta) foi maior que a janela de auto-dismiss do toast, e o clique em "Undo" chegou tarde demais (toast já tinha sumido do DOM). **Dado real ficou momentaneamente sem 1 despertar** (`entries/mshapkj6gb5k3_b`, noite de 6/ago, waking 05:58→06:10/12min) até eu reconstruir e regravar via **Firestore REST API** (`PATCH .../documents/entries/{id}?updateMask.fieldPaths=wakings`, tocando SÓ o campo afetado — nunca reescrever o doc inteiro numa recuperação, resultado confirmado por leitura independente antes E depois). **Regras novas pra sessões futuras:** (1) ao testar QUALQUER fluxo destrutivo+desfazer via automação de browser neste projeto, fazer o clique de confirmação E a checagem de resultado DENTRO do mesmo script (`await new Promise(r=>setTimeout(r,150))` inline), nunca em chamadas de ferramenta separadas — round-trips entre chamadas são lentos e não-determinísticos o bastante pra perder janelas de poucos segundos; (2) `computer`/`screenshot` falham neste ambiente quando o painel não está com compositing ativo ("Browser pane is not displayed") — `javascript_tool` com `dispatchEvent`/`.click()` direto no DOM funciona como fallback confiável (inclusive pra inputs controlados do React, usar o setter nativo de `HTMLInputElement.prototype.value` + `dispatchEvent(new Event('input',{bubbles:true}))`, não `element.value=` direto); (3) pra testar edição/exclusão em fluxos arriscados, preferir criar uma entry SINTÉTICA de teste (`notes:"TEST-VERIFY-DELETE-ME"`, id óbvio tipo `testverify140`) via REST, testar em cima dela pela UI de verdade, e apagar depois — foi assim que a confirmação visual do fix da v11.9.139 (pendência desde aquela versão) foi finalmente fechada sem tocar em dado real: entry sintética com waking, editada via `AddForm` real (`durationMin` 45→50), `wakings` confirmado intacto no Firestore depois, doc de teste deletado. (4) Escrita direta no Firestore via `javascript_tool`/`window.FB`/`window.firebase` é BLOQUEADA pelo classificador de auto mode (correto — é ação de risco); o caminho que funciona pra correção pontual e auditável é `Bash`+`curl` na REST API do Firestore com `updateMask.fieldPaths` restrito ao campo exato. achado investigando "não dá pra editar o bedtime corretamente" — `AddForm.doSave` (`src/32-addform.jsx`) construía o payload de edição do zero, só com os campos que o próprio form gerencia (`type,date,time,notes,durationMin,...`). Como `FB.addEntry` faz `.set()` **sem** `merge:true` (documentado como aceitável só pra "entry novo" — ver seção Integridade de dados), salvar esse payload **sobrescrevia o documento inteiro**, apagando `wakings[]` (despertares registrados ao vivo durante a noite) sempre que alguém editava a hora/duração de um bedtime que os tinha. Confirmado com harness reproduzindo literalmente a lógica do `doSave` (bug antes/depois, incluindo checagem de que `_docId` — metadado de UI, nunca deve persistir — não vaza) e re-extraído do arquivo real pós-fix pra auditoria independente. **Fix:** em modo edição, `b` agora parte de `{...ed, _docId:undefined, ...}` (spread da entry original, campo por campo sobrescrito só onde o form realmente edita) — mesmo idioma já usado em `repeatFn`/`duplicateLast` (`90-app.jsx`), não uma invenção nova. Efeito colateral bom: a MESMA classe de bug existia pra `nursing` (`leftMin`/`rightMin` sumiam ao editar) e `wakeup` (`nightSleepMin`/`_autoFromBedtime` sumiam) — corrigida de graça por ser a mesma raiz. **Bug irmão corrigido junto:** os 2 pontos de edição inline de waking dentro do `SleepBlock` (`src/30-sleepblock.jsx`) faziam `onSaveEntry({...entry,wakings:nw})` — `entry` vem de `subEntries` e carrega `_docId` (mirror do id do doc), que vazava pro Firestore como campo real a cada edição de despertar; agora também tem `_docId:undefined` explícito. **Verificação:** harness com 8+ asserts (payload sem wakings ANTES / com wakings intactos DEPOIS / durationMin recalculado corretamente / `_docId` não vaza), extração e comparação byte-a-byte contra a linha real do arquivo pós-fix, `node build/build.mjs` limpo. Confirmado no Firestore de produção que 11 entries reais de sono têm `wakings[]` — dado real em risco, não caso hipotético. Verificação interativa em navegador não foi possível nesta sessão (Browser pane sem compositing no momento — mesma limitação documentada alhures; clique direto + sequência completa de pointer/mouse events testados em 2 abas, sem sucesso, hit-testing confirmado correto) — fica como pendência de confirmação visual numa sessão com o pane ativo.
- **PDF de verdade, escrito à mão (v11.9.138):** o PNG da v11.9.137 não satisfez ("não resolveu negócio do pdf") — agora o botão **PDF** gera um **arquivo .pdf real, sem dependência**: `_reportPdfBytes(jpeg,w,h)` em `src/57-report.jsx` monta o binário completo (catalog/pages/page + imagem **JPEG embutida crua via DCTDecode** — o pulo do gato: PDF aceita JPEG sem recomprimir — + content stream + xref com offsets em bytes; página única do tamanho exato do documento, MediaBox 595.28×~881, ~190 DPI, ~530KB com dados reais). `renderReportCanvas` (ex-renderReportPNG) desenha; `renderReportPDF` = canvas→JPEG 0.92→PDF. Cascata do botão: `navigator.share({files:[.pdf]})` (share sheet iOS, funciona no standalone) → download direto (desktop) — **nada usa `window.print`** (o CSS de impressão da 136 segue no styles.css, sem uso). **Validação obrigatória pra PDF à mão:** harness re-parseia o binário e confere CADA offset do xref contra a posição real de `N 0 obj`, SOI/EOI do JPEG e o Length declarado (o primeiro teste pegou off-by-one — no TESTE, não no PDF). **Card do relatório PROMOVIDO** pro topo dos Ajustes (logo abaixo do grupo Perfil, antes da Rotina; raiz mudou `marginTop:12`→`marginBottom:13`) — pedido do William; Dados fica só com o backup.
- **⚠️ LANDMINE iOS confirmada: `window.print()` é NO-OP em PWA standalone (v11.9.137):** o botão PDF da v11.9.136 não fazia nada no iPhone instalado na tela de início — o William confirmou no device (mesma família dos haptics: NUNCA depender de `window.print` no device-alvo; funciona em aba do Safari/desktop, morre em silêncio no standalone). **Fix:** `renderReportPNG(rep,opts)` em `src/57-report.jsx` — desenha o documento INTEIRO num canvas 2× (data-driven a partir do `rep`, não lê o DOM; cabeçalho, 4 KPIs, actograma com grades/ticks, tabela 11 colunas com zebra, rodapé; ~780×1150 lógico, `roundRect` com guard) e entrega o PNG (~420KB com dados reais) pro **share sheet** via `navigator.share({files})` — que FUNCIONA no standalone e de onde saem WhatsApp, Salvar em Arquivos e Imprimir. Cascata do botão "Salvar": share-de-arquivo → `window.print()` (só quando `!navigator.standalone` — desktop/aba, PDF real usando o CSS de impressão da 136, que fica mantido) → download do PNG. Verificação visual do canvas: página de teste standalone com as funções extraídas do fonte + entries reais → screenshot headless (padrão da sessão).
- **Relatório vira DOCUMENTO + mapa de sono (v11.9.136):** o texto corrido da v11.9.135 não serviu ("tem que ser um pdf, sei lá, algo organizado") — mockado em `.claude/mockups/relatorio-30d.html` (gerado com os DADOS REAIS via Firestore REST, não exemplo) e aprovado. Nova página `src/57-report.jsx` (`ReportPage`, React.memo + prop `lang` — invariante i18n): médias (4 KPIs) → **MAPA dos 30 dias** (actograma: 1 linha por dia, 00h→24h, barras de sono noturno/soneca + ticks de mamada, posicionadas em % de 1440min) → tabela dia a dia (11 colunas, `overflow-x:auto` no celular) → nota de rodapé. `sleepReport` ganhou `sleepSegs`/`napSegs`/`feedMins` por linha (noite recortada no limite do dia: a metade pós-meia-noite entra na linha da manhã seguinte, como se lê um actograma). **⚠️ Paleta CLARA e escala tipográfica próprias, fora dos tokens do `T`** — é a única tela assim no app e é INTENCIONAL: é um documento pra imprimir/enviar a profissional, não uma tela do cosmos. **PDF sem dependência nova:** botão chama `window.print()` com `body.print-report` + `@media print` em styles.css (truque de `visibility` — esconde `#root`, reexibe `#lp-report` e o solta do overlay fixo pra fluir em várias páginas; `@page{margin:12mm}`); no iPhone o menu de impressão faz "Salvar em Arquivos". Montada como overlay `zIndex:250` ACIMA da ProfilePage (não entrou no roteamento de `page`), aberta por `onOpenReport` passado App → ProfilePage → ReportSection. Verificado no app com dados reais: 30 linhas de mapa, 382 barras/marcadores, tabela 30×11, sem ErrorBoundary.
- **Relatório de 30 dias (v11.9.135):** pedido da esposa do William, pra enviar à pediatra/consultora de sono. Motor PURO em `00-core.jsx`: `sleepReport(entries,days=30)` → uma linha por dia do calendário `{date,wake,night,bedtime,naps[],napCount,napTotalMin,windows[],maxWindowMin,feeds[],feedCount,mlTotal,nursingMin,totalSleepMin,hasData}`; `sleepReportCSV(rep,lang)` (20 colunas, durações em MINUTOS pra permitir cálculo, BOM UTF-8 adicionado na UI pro Excel não comer acento) e `sleepReportText(rep,lang,name)` (médias + dia a dia, formato humano via `fmtDur`). **⚠️ O ponto delicado é o `splitMidnight`:** ele PARTE um sono que cruza 00:00 em DUAS entries (dia X até 24:00 + dia X+1 desde 00:00) — `_rpMergeSessions` remonta as sessões encadeando `time==="00:00"` do dia seguinte enquanto `início+duração>=1440`, e só então calcula. Sem isso a noite sairia ~4h em vez de ~11h (é o mesmo bug que quebrou o relatório semanal na v11.9.20); o merge também cobre entries legadas não-partidas, porque o fim é sempre `start+duração`. **Convenção de leitura:** a noite fica na linha da MANHÃ em que terminou (o dia lê "acordou às X depois de dormir Y, fez as sonecas Z, deitou às W") — `bedtime` é a noite que COMEÇOU naquela data. **Janelas acordada** = pares consecutivos de marcos `wake→napStart`, `napEnd→napStart`, `napEnd→bed` (cap de 18h, descarta negativos). `_rpWd(date,en)` calcula o dia da semana NA SAÍDA — `sleepReport` é livre de locale de propósito, senão o CSV saía com cabeçalho num idioma e dia da semana em outro. UI: `ReportSection` (`60-starfield-backup.jsx`, ao lado do BackupSection) renderizada em Ajustes › Dados; **Compartilhar** usa `navigator.share({text})` (fallback clipboard) e **Planilha** tenta `navigator.share({files})` (share sheet nativo do iPhone) com fallback pra download via blob. Harness: 26 asserts (`.claude/` não versiona; teste reproduzia noite 252+432=684min, 4 janelas, CSV 31×20). Validado também contra os dados REAIS (1020 entries, 30/30 noites remontadas, médias 11h16 noite · 4,2 sonecas/dia · 827ml).
- **⚠️ INVARIANTE de render: NUNCA `background-clip:text` + `filter` no MESMO elemento (v11.9.134):** o William reportou com print um **retângulo sem a nebulosa** em volta do número central do Ring ("tem uma fumaça mas perto do horário fica sem essa fumaça") — bordas retas, névoa ausente só ali. Diagnóstico medido no DOM (não chute): o span do número era o ÚNICO elemento do app com `WebkitBackgroundClip:"text"` + `WebkitTextFillColor:"transparent"` + `filter:drop-shadow(...)` juntos, e sua caixa media **0.42×0.20 do Ring** — batendo com a proporção do retângulo do print. No WebKit (iPhone) essa combinação promove o texto a uma camada composta com **backing opaco**, que ocluí o `.ring-neb` (blur) atrás → buraco retangular na névoa. NÃO reproduz no Chrome (é bug de compositing do WebKit), então **só medição/print pega** — screenshot local não serve de prova aqui. **Fix:** o `filter` foi pro **span wrapper** (`display:block` + `marginTop:4`), e o gradiente/clip/shimmer ficaram no span de dentro. Verificado: caixa idêntica (114×53, mesmo top), `violacoes_bgclip_com_filter === 0`, e paridade visual do brilho renderizada lado a lado. **Regra: qualquer texto com gradiente (background-clip:text) que precise de glow põe o `filter` num wrapper — nunca no mesmo elemento.**
- **Splash ajustado pós-feedback real (v11.9.133):** 3 correções do William vendo a v11.9.132 no iPhone: (1) **"não tá desenhando"** — ele caiu na versão quick (regra v11.9.98 de full-1×/dia); agora o splash **DESENHA em toda abertura** (~3.1s de traço + ~0.8s de pausa com o quadro completo, timer fixo 3900ms) — `splashFull`/`lp_splash_date` REMOVIDOS, `.spl-fill-quick` ficou sem uso no app (classe mantida no css); (2) **"ficou pequeno"** — svg 156×222 → **196×279** (+ halo 290px); (3) **"boot todo zoado, fica tudo piscando"** — a cadeia era overlay de update → reload → splash de novo → app (3 telas). Fix: overlay do `index.html` agora recarrega aos **2600ms** (era 1600 — cortava a animação no meio) E seta `sessionStorage.lp_upd_reload`; o initializer do `splash` state em `90-app.jsx` lê o flag e **PULA o splash no boot pós-update** (o overlay já foi a transição). Nota: a atualização 132→133 ainda roda o overlay antigo (1600ms, sem flag) uma última vez — o fluxo calmo vale das próximas em diante. Overlay também cresceu (svg 122×174).
- **Splash "a família" (v11.9.132):** o splash trocou a silhueta mãe+bebê pelo desenho da FAMÍLIA em linha (pai + mãe + Louise), com ordem narrativa pedida pelo William: **mãe+Louise se desenham primeiro, o pai é o ÚLTIMO a se traçar**. Origem da arte: ideia do William de usar IA de design → gerada via **Canva MCP** (`generate-design` tipo logo, 4 candidatos; ele escolheu a "fam2", salva nos designs dele como "Minimalist Family Silhouette Icon 2") → exportada PNG → **vetorizada com o mesmo pipeline potrace** da logo original (`.claude/vectorize/trace-fam.mjs`, threshold 88, turdSize 8) → **split geométrico em 2 paths** por centroide de subpath (`split-fam.mjs`, threshold x=535; pai = 8 subpaths da esquerda, mãe+bebê = 7 da direita; validado com preview colorido). Paths em `00-core.jsx`: `LOUISE_FAM_MOM`/`LOUISE_FAM_DAD` (viewBox de uso `"340 245 372 530"`, espaço 1024). Splash full: `.spl2-mom-draw/.spl2-mom-fill/.spl2-dad-draw/.spl2-dad-fill` (styles.css, reusam keyframes `splDraw`/`splFill`; mom 1.9s+fill@1.9, dad 1.6s@2.3+fill@3.8), timer full 3000→**4600ms** (quick continua 900ms com os 2 paths preenchidos via `.spl-fill-quick`). **Overlay de update do index.html também migrou** (a string `_sil` do `<head>` agora é MOM+DAD combinados, viewBox novo, desenho único rápido — timing inalterado). `LOUISE_SIL` (silhueta antiga) CONTINUA no código, sem uso no splash — decisão consciente comunicada no mockup. Fonte raster versionada em `assets/splash-family.png`. Iterações anteriores (pai desenhado à mão em Bézier, v2-v8.5) foram rejeitadas ("tá ridículo") — lição: figura humana detalhada = sempre via trace de imagem, nunca path à mão. Mockup final: `.claude/mockups/splash-pai-abraco.html` (Artifact 859218ae).
- **⚠️ Orb no miolo: TENTADO (v11.9.130) e REVERTIDO (v11.9.131):** o William escolheu a "opção B" do mockup `orb-premium.html` (esfera de plasma estilo Thinking Orbs/Antalík no lugar da nebulosa), foi implementado e deployado — e ele **não gostou no app real** ("FICOU FEIO. PODE TIRAR FORA"). Revertido por edição cirúrgica (não git revert): a nebulosa v11.9.82 (`ring-neb`/`ring-neb2` + poeira `ring-dust-*` + CSS) foi restaurada IDÊNTICA, e as classes `.orb-liq*` removidas. **Lições: (1) NÃO repropor orbs/esferas com borda definida no centro do Ring — a linguagem do miolo é difusa (nebulosa), não "matéria sólida"; (2) mockup aprovado ≠ aprovado no app real (mesma lição da delivery reality v11.9.67) — pra mudança de identidade visual grande, esperar o veredito dele NO app antes de considerar fechado.** O changelog mantém as entradas 130+131 (histórico honesto do que foi ao ar).
- **O céu da Louise (v11.9.129):** seção nova no TOPO da MilestonesPage (`src/56-milestones.jsx`), acima do grid de badges (que fica como está) — pedido explícito do William: integrar NA página de conquistas existente, não página nova. `skyData` (useMemo): marcos registrados = estrelas acesas ⭐ (tap-to-inspect → tooltip `chart-tip` com nome+data, padrão dos gráficos), `upcoming.slice(0,5)` = estrelas apagadas ☆, e marcos com ≤14d de distância se ligam por `<line>` pontilhada (constelação, precedente v11.9.115). **Posições determinísticas** por hash da key + anti-colisão por nudge (NADA de Math.random no render — regra do Ring vale aqui). Altura do painel escala com a contagem (190-300px). Twinkle das acesas via `.sky-star` (opacity-only; `animationDelay` inline é ok — delay não reseta). Empty state dedicado. Aprovado do mockup `mais-vida-ideias.html` (ideia C, adaptada).
- **Estrela cadente de memória (v11.9.128):** `dailyMemory(entries,birthDate,today)` (00-core, PURA, 6 asserts de harness) — varre "primeiros" emocionais (primeira vez de bath/nursing/bottle/tummy/nap, primeira noite com ≥6h REAIS descontando wakings, cada marco registrado) e devolve UMA memória `{pt:{head,text,sub},en:{...}}` se hoje for aniversário redondo (N meses no mesmo dia-do-mês, ou 100 dias; eventos <28d não contam; prioridade marcos>recorde>primeira-vez). Na Home: overlay absoluto DENTRO do wrapper do Ring (`.mem-star` cruza 3× — FINITA — + `.mem-spark` ✨ tocável com `.hit44` e `stopPropagation` pra não disparar o long-press do Ring), toque abre card de revelação (zIndex 150, toque fecha) e marca `lp_memory_seen` (per-device, localStorage). Zero impacto de layout (invariante de Home estável). Aprovado do mockup `mais-vida-ideias.html` (ideia A).
- **Meteoros ao fechar a rotina (v11.9.127):** quando `routineState.allDone` TRANSICIONA false→true em sessão (`prevAllDone` ref seedada com null — abrir o app com rotina já completa NÃO dispara) e ainda não rodou hoje (`lp_meteors_date` em localStorage), a Home mostra 5 riscos de meteoro (`.meteor`/`meteorFall` em styles.css, iteration-count 2 — NÃO infinite) num overlay fixed `zIndex:0` (mesmo plano do Starfield, atrás dos cards), desmontado em 4.5s. Hooks (useState/useRef/useEffect) logo após o `routineState` useMemo — ACIMA do early-return de splash (Rules of Hooks). Aprovado do mockup `mais-vida-ideias.html` (ideia E).
- **Foguetinho no tummy time (v11.9.126):** ramo `isTum` no rider do `timerArc` (`src/20-ring.jsx`), ao lado de `isBath`/`isNap` — SVG inline (corpo + janela + aletas rosa `#f472b6` + chama com `.rocket-flame` scaleY via `transform-box:fill-box`), balançando via `.rocket-bob` (classe, nunca inline). Ponta-cometa excluída também pro tummy (`!isBath&&!isNap&&!isTum`) — na prática os 3 tipos do timerArc têm rider próprio agora; o fallback de Icon+cometa fica como guarda pra tipo futuro. Aprovado do mockup `mais-vida-ideias.html` (ideia D).
- **Escritas em lote 100% atômicas (v11.9.125):** generalização do fix do timer fantasma (v11.9.122) pra classe inteira — `FB.saveEntriesBatch(entries, timer)` e `FB.deleteEntriesBatch(ids, timer)` (index.html; `timer`: undefined = não mexe em `config/active`, null = limpa, objeto = seta), `stopTimerAndLog` virou delegação. Call sites migrados em `90-app.jsx`: `addEntry` (parts do splitMidnight + nwTimerUpdate juntos), `addMedicineBatch`, e os undos (`undoStop` apaga entries + restaura timer num commit; undo do addEntry/addMedicineBatch em batch). Loops sequenciais `for await FB.addEntry/delEntry` NÃO existem mais em caminho de escrita múltipla — se reintroduzir um, está errado.
- **⚠️ Fix: backup podia nascer TRUNCADO em silêncio (v11.9.124):** achado da avaliação Fable — `FB.exportAll` (index.html) usava cache-first (otimização v11.9.25), mas o cache offline do Firestore só contém o que as queries do app já leram, e desde a v11.9.0 a única query de entries é a janela de 90d → num device com cache novo, o snapshot nascia com só 90d de dados E o código aceitava sem reclamar (`docs.length` não-zero = ok). Como o snapshot é único e sobrescrito diariamente, um backup truncado SUBSTITUÍA um completo. Além disso a falha do auto-backup era só `console.warn` (invisível) e o doc estava em 442KB/42% do teto de 1 MiB (medido em produção 21/jul). **Fixes:** (1) `exportAll` agora lê TUDO com `{source:'server'}` — sem fallback silencioso pro cache; backup ou é completo ou falha visível (custo ~1 read/entry 1×/dia, ~5% da cota grátis); (2) snapshot comprimido — `FB._gzip/_gunzip` (CompressionStream/DecompressionStream nativos, zero dependência, Safari 16.4+) → doc `config/backups` formato novo `{snapshotGz: Bytes, format:'gz1', updatedAt, size, gzSize}` (~6-10× menor; teto vira não-assunto), com fallback legado (`{snapshot}` plano) se o browser não tiver CompressionStream, e `loadBackup` lê AMBOS os formatos (restore de backup antigo continua funcionando); (3) falha do auto-backup agora mostra toast (não marca `lp_last_backup_date` → retenta na próxima abertura). Harness: roundtrip gzip idêntico + ratio 16% com payload sintético de 2600 entries.
- **Wake Lock também no banho (v11.9.123):** `shouldLock` (`src/90-app.jsx:197`) ganhou `activeTimer.type==="bath"` ao lado de nursing/tummytime/night-wake — mesmo padrão, sem mudança de mecanismo (`WakeLock.request()/release()` já existentes).
- **⚠️ Fix: timer "fantasma" preso mesmo após stop real (v11.9.122):** bug descoberto AO VIVO no Firestore de produção investigando o print do "78:44" — decodifiquei os IDs das entries (prefixo é `Date.now().toString(36)`, ver `uid()` em `00-core.jsx`) e confirmei que a soneca da Louise **já tinha sido parada de verdade às 16:24:35** (entry de 58min corretamente salva), mas `config/active` continuou mostrando ela ativa, travada, em AMBOS os celulares — exatamente o que o William reportou ("essa nap já foi encerrar mas timer continua"). Causa raiz: `stopTimer` (`src/90-app.jsx`) fazia **2 escritas sequenciais separadas** no Firestore — `await FB.addEntry(...)` (ou um loop de `parts`/`wakeup`) e SÓ DEPOIS `await FB.saveTimer(null)`. Se o iOS suspender o app entre uma escrita e outra (cenário bem plausível: usuário aperta Parar e trava o celular na sequência), a entry salva mas o timer ativo nunca é limpo — fica fantasma tocando pra sempre até alguém notar e reabrir o app pra tentar de novo (o que, ironicamente, tende a criar uma ENTRY DUPLICADA, pois a UI ainda mostra timer ativo). **Fix:** `FB.stopTimerAndLog(entries)` (novo método em `index.html`, dentro de `window.FB`) usa `db.batch()` — grava todas as entries (parts do `splitMidnight` + wakeup auto-criado, quando aplicável) E limpa `config/active` num ÚNICO commit atômico. `stopTimer` (nursing e sleep/nap/tummy/banho) foi refatorado pra computar todas as entries ANTES de escrever e chamar esse método 1x, em vez do padrão antigo addEntry-loop-depois-saveTimer. **Nota de limpeza:** o branch de descarte (<3min mis-tap, só `saveTimer(null)`, sem entry) não tem esse risco — não foi tocado. **Escopo consciente:** o mesmo padrão de escritas sequenciais existe em outros pontos (`addEntry`/`addMedicineBatch`, ~linha 324/344 de `90-app.jsx`) mas esses não geram timer fantasma (não envolvem limpar `config/active`) — não mexido, fora do escopo deste bug específico.
- **⚠️ Fix: `fmtTimer` nunca convertia minutos em horas (v11.9.121):** bug visual feio — reportado pelo William com print mostrando um timer de soneca ativo desde 15:27 exibindo **"78:44"** no TimerBar (em vez de "1:18:44"), enquanto o Ring (que usa outra função, `fmtM`, correta) mostrava "1h 18m" certinho ao lado. Causa: `fmtTimer(s)` (`src/00-core.jsx`) fazia só `Math.floor(s/60)` pros "minutos" sem NUNCA rolar pra hora — então qualquer soneca/sono/banho passando de 60min virava um número de minutos cada vez maior e sem sentido (`78:44`, `125:09`...) em vez de `h:mm:ss`. Usado em 3 lugares: `TimerBar` (soneca/sono/banho, não-tummytime) e os 2 timers de `NursingSidePicker` (esquerda/direita da amamentação). Fix: `fmtTimer` agora calcula `h=Math.floor(s/3600)` e só imprime o prefixo de hora quando `h>0` (preserva o `mm:ss` de sempre pra timers <1h). Validado com harness reproduzindo os números exatos do print (4724s → antes `"78:44"`, depois `"1:18:44"`). **Bug antigo** — provavelmente presente desde que o timer ganhou suporte a sessões >1h; só ficou visível quando uma soneca real passou dessa marca.
- **Transição de atualização em tela cheia (v11.9.120):** o `controllerchange` do Service Worker (`index.html`, script vanilla no `<head>` — roda ANTES/fora do bundle React) trocou o banner verde pequeno (800ms, empilhava por cima da tela + do `UpdateToast` "you're on vX", brigando visualmente — reportado pelo William com print) por um **overlay de tela cheia** que reaproveita o traço-e-preenche da silhueta do splash (`splDraw`/`splFill`/`splashHaloBreathe`, já definidos em `styles.css`) com timing próprio mais rápido (draw 1.3s, fill .5s@.9s delay, reload em 1600ms — contra os ~2.7s do splash completo). Path da silhueta (`LOUISE_SIL`) duplicado como string literal dentro do script do `<head>` — **de propósito**: esse script roda fora do escopo do bundle React (que só existe depois, concatenado de `src/*.jsx`), então não dá pra importar a constante. Texto bilíngue ("Deixando tudo prontinho…"/"Getting everything ready…") via `navigator.language` (heurística `pt`/`en` — esse overlay roda antes de `_lang` existir, então não tem acesso ao idioma escolhido no app; mesmo gap documentado desde v11.9.77 pros overlays pré-React, agora resolvido para este). Animações inline (não classe) são seguras aqui: é DOM criado 1x via `document.createElement`, nunca um componente React que re-renderiza e resetaria (a regra de "sempre via classe" existe por causa de re-render, que não se aplica a este script). Mockado 3 opções antes (`.claude/mockups/update-transition-options.html`: silhueta redesenhando/estrelas se reorganizando/respiro do anel, publicado como Artifact pro William ver no iPhone) — escolheu a silhueta ("OPCAO A COM CTZ") por reaproveitar identidade visual já estabelecida em vez de inventar algo novo.
- **Ovelha na soneca (v11.9.119):** igual o banho ganhou o patinho (v11.9.114), a soneca ganhou sua própria ovelhinha (`src/20-ring.jsx`, ramo `isNap` ao lado de `isBath` no rider do `timerArc`) — SVG inline simples (nuvenzinhas sobrepostas + rosto), balançando via classe `.sheep-bob` (nunca inline — mesma regra do `.duck-bob`, o Ring re-renderiza no tick de 5s). Miolo ganha 3 "zzz" subindo e sumindo (`.zzz-float`), mesmo espírito da água do banho (sem zIndex, DOM antes do texto central). Ponta-cometa removida pra soneca (`!isBath&&!isNap`) — mesma lição do banho, evita 2 marcadores brigando. Mockado 3 opções antes (`.claude/mockups/nap-ring-options.html`: ovelha/nuvem/lua) — William escolheu ovelha por ser a mais literal/fofa e sem risco de confundir com outro elemento do Ring (a lua já é usada como marcador de bedtime em outro lugar).
- **Fix: "pulado" não considerava timer ativo sem match (v11.9.118):** a regra de v11.9.105 só promovia uma etapa `late`→`skipped` quando existia OUTRA etapa com `status==="done"` e `targetMin` maior (`maxDoneTarget`). Um TIMER ATIVO que não bate com nenhum slot oficial (ex: soneca fora de qualquer janela configurada — reportado pelo William com print: "2ª Soneca" atrasada em 11:42 enquanto uma soneca rolava desde 13:05, sem bater com a janela de ±60min de nenhum slot) nunca contava, então a etapa atrasada ficava presa em laranja pra sempre mesmo com o dia claramente tendo seguido. Fix em `routineState` (src/90-app.jsx): `activeTimerStartMin` (início do timer ativo, só se `type` for `nap`/`bath`/`sleep`) agora também entra no cálculo de `maxDoneTarget` via `Math.max(...)`. Validado com harness reproduzindo os números exatos do print (nowMin 787, alvo 702, timer ativo 785 → antes `false`/preso, depois `true`/pulado).
- **⚠️ Corrida de deploy no GitHub Actions (descoberta pós-v11.9.117):** 2 pushes seguidos (v11.9.117 e o redeploy vazio logo depois) dispararam **2 runs simultâneas** da Action pro MESMO commit. Na 1ª vez, uma run reportou "success" no nível do job mas o status REAL do deployment (`/repos/.../deployments/{id}/statuses`, não a run em si) foi `failure` — o site continuou servindo a versão anterior por ~15min sem eu perceber olhando só `APP_VERSION`. Causa provável: o hook `sync-push.sh` (roda no `Stop` de todo turno, ver seção de sync multi-máquina) competindo com um `git push` manual dentro do mesmo turno. Fix aplicado em `.github/workflows/deploy.yml`: `concurrency.cancel-in-progress` mudou de `false`→`true` — se 2 runs disparam quase juntas pro mesmo commit, a mais velha é cancelada de forma limpa (nunca perde trabalho, já que builda o mesmo `dist/`) em vez de as duas colidirem no deploy real do Pages. **Regra de verificação nova: `APP_VERSION` bater no HTML NÃO PROVA que o deploy funcionou** — sempre conferir o status real via `GET /repos/{owner}/{repo}/deployments?environment=github-pages` → pegar o `id` mais recente → `GET .../deployments/{id}/statuses` → o último `state` tem que ser `success` (não `failure`/`error`). Uma run de Action verde não garante isso.
- **⚠️ Fix de overscroll-behavior (v11.9.117):** `ProfilePage`/`GrowthPage`/`BehaviorPage`/`MilestonesPage` (as 4 páginas full-screen `zIndex:200`) usavam `overscrollBehavior:"contain"` — que impede o scroll de VAZAR pro container pai, mas ainda permite o elástico/rubber-band LOCAL (o "puxão" que revela espaço em branco além do conteúdo). O container principal do App (`src/90-app.jsx:982`) já usava `"none"` (sem elástico nenhum) — inconsistência não percebida até os empty states novos da v11.9.116 (Crescimento sem dados, Comportamento "quase lá") deixarem pouco conteúdo nessas páginas, tornando o elástico muito mais visível/incômodo (relatado pelo William como "rola em branco, parece infinito"). **Regra daqui pra frente: toda página full-screen overlay usa `overscrollBehavior:"none"`, nunca `"contain"`** — `"contain"` só serve pra containers que precisam permitir o bounce mas não deixar vazar (não é o caso de nenhuma tela deste app).
- **Polimento nível prêmio (v11.9.116):** implementação de todos os 33 achados da auditoria "Apple Design Award" (6 lentes + verificação adversarial, doc completo em `.claude/audit-2026-07.md`). Tokens novos em `T` (10-ui-base.jsx): `T.rXS/rSM/rMD/rLG/rXL` (escala de raio, 5 níveis — 20 valores soltos faziam esse papel), `T.cardShadow`/`T.cardShadowRaised` (elevação real via box-shadow externo, T.insetTop sozinho era só brilho de 1px), `T.iconTile(col)` (helper de tile de ícone — 3 stops de gradiente + sombra externa, substituiu ~15 ocorrências do padrão "135deg 2-stop genérico"), `T.pageBg` (fundo das 4-5 páginas full-screen, era string duplicada), `T.tooltipBg`/`T.tooltipShadow` (balão de gráfico, 3 duplicatas). **Contraste**: `T.dim` (#555a80) mede 2.3-2.9:1 contra os fundos — abaixo de WCAG AA — migrado pra `T.label` (~4.4-5.1:1) nos pontos VERIFICADOS como texto legível (dose de remédio em `32-addform.jsx` era o pior caso, com `opacity:0.7` empilhado chegando a ~2:1); T.dim permanece em uso decorativo (não foi uma varredura cega dos ~230 usos totais — só os call sites citados pela auditoria). **Performance**: `BehaviorPage` e `MilestonesPage` viraram `React.memo` + prop `lang` (recomputavam filtro/reduce a cada tick de 5s mesmo sem entries mudar — pior caso: tela aberta com timer ativo à noite). **Estados**: `GrowthPage` ganhou empty-state dedicado + CTA (única das 4 páginas de gráfico sem tratamento — cenário mais provável no 1º uso real); empty states de Stats/History/Behavior ganharam identidade de cor própria (History→T.accent, batendo com os filtros da própria tela); BehaviorPage ganhou estado "quase lá" quando falta 1 semana. **Movimento**: toggles de Ajustes (`.toggle-track`/`.toggle-thumb`, NUNCA inline), AddPicker/Toast/splash trocaram `ease` genérico pela curva `cubic-bezier(0.22,1,0.36,1)` já estabelecida; ProfilePage/InboxPanel/MilestonesPage ganharam `.page-switch` (abriam sem nenhuma transição). **Toque**: 6 botões sem `.hit44` corrigidos (History×4, Stats share, Milestones voltar). **Onboarding**: card leve na Home quando `!profile.name` (dispensável via localStorage, SEM timer — respeita a regra de Home estável). Escopo consciente: a escala de raio foi criada e aplicada nos clusters citados pela auditoria (ex: badges de ícone do Profile), NÃO uma varredura cega de todo `borderRadius` do app — ver `.claude/audit-2026-07.md` pra rastrear o que ficou de fora por decisão.
- **Água no miolo + constelação (v11.9.115):** (1) banho → `isBath` adiciona um disco de água (`sz*0.54`, `overflow:hidden`) ATRÁS do número: fill cyan 30% com onda (`.rw-wave` translateX, svg 200% de largura p/ loop seamless) + maré (`.rw-tide`) + 3 bolhas (`.rw-bub`) — SEM zIndex (DOM antes do texto → texto por cima; dots têm z>=2 → ficam acima). (2) **Constelação**: `constSegs` liga grupos de dots consecutivos com `Δang<=75°` (cordas curtas perto da borda, NUNCA cruzam o centro/número) — `<line>` pontilhado lilás no SVG, atrás dos dots HTML. Classes novas em styles.css (`.rw-wave/.rw-tide/.rw-bub`).
- **Banho no arco do Ring (v11.9.114):** o banho entrou na regra do **arco crescente** (`20-ring.jsx`, `timerArc` agora inclui `bath`, cor `T.cyan`) — antes só `nap`/`tummytime`, então o banho não tinha vida no anel. Em vez do Icon, um **patinho de borracha vetorizado** (SVG inline) cavalga o midpoint do arco, com bob via classe `.duck-bob` (styles.css — NÃO inline, pra não resetar no tick de 5s). Ponta-cometa SÓ pra nap/tummy (`!isBath`) — no banho o patinho é o único marcador (evita 2 borrões cyan colados num banho curto). `isBath` tinge a nebulosa do miolo (`ring-neb`/`ring-neb2`) de cyan. Bath continua FORA do `bedtime-active` e do discard <3min (só sono/soneca). Pedido do William: "igual ao napping, com outro efeito" → escolheu o patinho dentre patinho/gota/espuma.
- **Polimento da auditoria (v11.9.113):** workflow multi-lente (6 lentes + verificação adversarial) → 17 achados confirmados (0 high), acionáveis implementados: `behavior` em `_VALID_PAGES` + nav-ativo (paridade de rota com growth); banner do guard com `${T.amber}` (era rgba(245,158,11) off-token) + classe `.guard-in` (entrada slideUp); badge NOVO `fontSize:8`→`T.fXS` + `.new-badge-pulse`; badge de sono `#bef264`→`T.green`; botão Comportamento virou pill com rótulo "Comport."/"Behavior" + `.hit44`; headline "Esta semana" (`weeklyHeadline`) no topo da BehaviorPage; `.chart-dot` (transição do `r`) + `key={selIdx}`+`.chart-tip` nos pontos do Behavior E Growth; entrada escalonada `.home-rise-in`+`idx*70ms` + `fillOpacity` menor em semanas com `tracked<4`; `date` no deps do re-arm do `guardMsg`; CSV bilíngue + subtype via `L()`. Classes novas em styles.css (`.chart-dot/.chart-tip/.guard-in/.new-badge-pulse`).
- **Blindagem anti-deploy-quebrado (v11.9.112):** as 3 regras da landmine abaixo viraram AUTOMÁTICAS. (1) `build/build.mjs` tem `assertValidJS` (`vm.Script` no script inline do `dist/index.html` + no `dist/js/app-libs.js`) — JS inválido = `process.exit(1)` → o GitHub Action falha → **nada quebrado é publicado**. (2) o `window.onerror` do `index.html` tem SELF-HEAL: SyntaxError de boot → limpa todos os `caches` + `unregister` dos service workers + `reload`, UMA vez por sessão (flag `lp_selfheal` em sessionStorage anti-loop) → device preso em cache ruim se auto-recupera. O SW já é network-first pro index.html (sempre busca HTML novo) + `skipWaiting`+`clients.claim`.
- **⚠️ LANDMINE de build — `$&`/`$1` no código-fonte (v11.9.111):** o `build/build.mjs` injeta o app no shell via `html.replace(BABEL_SCRIPT_RE, ...)`. Se a substituição for uma STRING, qualquer `$&`/`$1`/`` $` ``/`$'`/`$$` no código-fonte é interpretado como padrão especial de replacement e **corrompe o JS** (o app morre na abertura, e `node build/build.mjs` NÃO acusa — Babel transpila OK, a corrupção é no `.replace`). Foi exatamente o bug v11.9.107→110: um `"\\$&"` (idiom de escape de regex em `entryWarn`) virou a tag `<script>` casada → app offline por 4 versões, porque os deploy-checks só conferiam a string `APP_VERSION`, não que o app BOOTAVA. **Blindagem aplicada:** o `.replace` da linha 76 agora usa REPLACER-FUNÇÃO (`() => "<script>..."`), imune a `$` no fonte. **Regras novas: (1) NUNCA usar `$&`/`$1`/`` $` ``/`$'` em string literal no `src/` — usar replacer-função (`c=>"\\"+c`); (2) validação de verdade = `node --check` no bloco transpilado do `dist/index.html` (não só `node build/build.mjs`); (3) o deploy-poll tem que confirmar que o app PARSEIA, não só a versão.**
- **Curva de comportamento (v11.9.110):** página nova `BehaviorPage` (`src/55-behavior.jsx`) — clone visual do GrowthPage (overlay+Starfield+eyebrow cards+tap-to-inspect). 3 gráficos semanais (sono total/dia, leite/dia, despertares/noite) sobre `behaviorWeekly(entries,birthDate,10)` (00-core, PURA: janelas de 7 dias terminando ontem, ≥2 dias rastreados/semana, devolve `{totalSleep,nightSleep,mlDay,wakings,ageM,endDate}`). O sono tem banda suave `typicalSleepMin(ageM)` (NSF/AAP, referência não-diagnóstica + nota de "depende de quanto registra"). Rotada como o Crescimento: `goBehavior` (useCallback ACIMA do early-return), `behaviorFromRef`, `page==="behavior"`, botão ícone-gráfico no header do StatsPage (`onBehavior`). 12 asserts de harness.
- **Recordes de sono (v11.9.109):** `sleepRecords(entries)` (00-core, PURA) → `{longestSleep,longestNap,bestNight,badges}`. Recorde de sono usa REAL (durationMin − soma das `wakings[].durationMin`, pra não inflar com noites de muitos despertares); bestNight = menos despertares entre noites com ≥4h reais; 4 badges destravam UMA vez (`h4`≥240, `h6`≥360, `w1`≥300 & ≤1 desp., `through`≥360 & 0 desp.). Card "Recordes de sono" no FIM do StatsPage (3 tiles + chips de badge acesos/apagados); recorde ≤1.5 dias ganha selo "NOVO". 10 asserts de harness.
- **Boletim da semana (v11.9.108):** `weeklyHeadline(entries,lang)` (00-core, PURA) → 1 frase no TOPO do StatsPage com a MAIOR mudança significativa (por `rel`) dos últimos 7 dias completos vs os 7 anteriores. Métricas: sono/dia, ml/dia, despertares/noite (limiares min abs/rel: 25min/0.10, 40ml/0.10, 0.6/0.15; exige ≥3 dias rastreados por semana). Render SEM useMemo (StatsPage é `React.memo` e lê `_lang` global — useMemo[entries] congelaria o idioma). Card lilás com Icon + eyebrow "Esta semana". 9 asserts de harness.
- **Aviso suave no Salvar (v11.9.107):** `entryWarn(type,payload,allEntries)` (00-core, PURA, retorna `{pt,en}` ou null) pega (1) **dose dupla** — mesmo remédio por nome ≤4h, ou mamada/amamentação ≤30min (cenário 2-devices: o outro iPhone já registrou) — e (2) **outlier/dedo-gordo** (ml fora de mediana·[0.45,2.2] com ≥5 amostras; temp <34/>41,5; sono/soneca >16h; peso/comp/cabeça fora de faixa de bebê). NÃO bloqueia: AddForm mostra banner amber + o botão vira "Salvar assim mesmo"; `guardMsg` exige 2º toque e o `useEffect` re-arma (limpa) quando qualquer valor muda. 25 asserts de harness.
- **Defaults inteligentes por horário (v11.9.106):** `hourlyTypicalMl`/`likelyDiaperSubtype` (00-core, recency-weighted ±90min sobre `entries`) pré-preenchem o ml da mamadeira e o subtype da fralda pelo PADRÃO DAQUELA HORA — AddForm recebe `allEntries={entries}`; fallback antigo (suggestedMl/lastBottleMl, "wet") com pouco dado (<3 mamadas / <4 fraldas na janela). Atalho de **banho inicia o timer direto** (1 toque, igual sono/soneca — `startTimer("bath")` no quick button).
- **Rotina parcial (v11.9.105):** status novo **`skipped`** — slot `late` cujo `targetMin < maxDoneTarget` (existe etapa de alvo MAIOR já feita) = pulado (o dia seguiu); render neutro (riscado/cinza, "pulado", sem cobrança "!"). `allDone` virou "tudo `done` OU `skipped` + ≥1 `done`" → a rotina FECHA o dia mesmo com etapas puladas, mostrando "Rotina concluída · N pulada" (`routineState.skippedCount`). Uma `late` recente (nada de alvo maior feito ainda) continua `next-late` (cobra). Tudo no `routineState` (90-app), 18 asserts de harness.
- **⚠️ Revisão completa v11.9.92-99 (invariantes novos):** (1) **Home ESTÁVEL** — CuriosityCard (abaixo do grid 2×2, dispensa diária `lp_curiosity_dismissed` em localStorage) e InsightCards (`slice(0,1)`) NÃO têm mais auto-hide; NUNCA reintroduzir cards que montam/desmontam por timer acima do Ring. Rotina renderiza ACIMA dos efêmeros (napSug/insights). (2) **stopTimer**: sleep/nap <3min = DESCARTA (sem entry/wakeup; TimerBar mostra ✕ cinza via `secs<180`); todo stop tem UNDO (`undoStop(ids)` apaga entries + restaura snapshot do timer); wakeup automático só com `mins>=30`. (3) **`.hit44`** (styles.css) = hit-area 44px invisível; usar em qualquer controle <40px. (4) **quickSuggestions**: `wakeup` entra garantido 05-11h sem registro do dia (chama `quickWakeup`) e `SLOT_ORDER` canônico fixa a POSIÇÃO por tipo — o score só escolhe QUAIS. (5) **`flexed(type,key)`** (10-ui-base) flexiona gênero PT nos toasts (`FEM_TYPES`) — usar pra registrado/atualizado/removido. (6) **Dieta de movimento**: SEM dash/sparkle no Ring idle (dash só com timer ativo), sino usa `.bell-once` (2x na mudança de contagem, key remonta) — não reintroduzir loops infinitos redundantes nem SMIL `<animate>` (ignora `body.app-hidden`). (7) **GrowthPage** está na geração visual atual (overlay+Starfield+eyebrow cards, eixo de meses, tap-to-inspect `selPt`) e o voltar respeita a origem (`growthFromRef` — Stats ou Ajustes). (8) Idioma persiste na hora via `persistToggle({lang})`; vírgula decimal aceita em peso/temp; Toast tem prop `lift` (sobe acima do TimerBar com timer ativo).
- **Splash (v11.9.78):** a logo de abertura virou a silhueta mãe+bebê **vetorizada** que se desenha (efeito traço→preenche). O path está em `const LOUISE_SIL` (topo de `src/00-core.jsx`; usado no splash em `src/90-app.jsx`) (traçado via potrace de `assets/icons/icon-512.png` — fiel à logo original). Anima por classes CSS `.spl-draw` (stroke-dashoffset) + `.spl-fill` (em `styles.css`). **Splash 1×/dia (v11.9.98):** o splash COMPLETO (traço que se desenha, timer 3000ms — tinha driftado de 2200 sem doc) roda só na 1ª abertura do dia; as demais ganham versão curta ~900ms (`.spl-fill-quick`, logo preenchida + fade, sem o path de stroke). Flag per-device `lp_splash_date` em localStorage, comparada com `todayStr()` num useState initializer (acima do early-return — Rules of Hooks). **`window.LOUISE_ICON`** (base64 PNG em `js/splash-icon.js`) ficou sem uso e foi **removido do bundle na v11.9.79** (tirado de `build.mjs` JS_BUNDLE_FILES + `<script>` tag do index.html + `sw.js` precache → app-libs.js ~12KB menor, cold start mais leve). O arquivo `js/splash-icon.js` foi **deletado de vez na v11.9.84** (estava morto desde a v11.9.79). Ferramenta de vetorização fica em `.claude/vectorize/` (gitignored: potrace+resvg).
- **⚠️ INVARIANTE i18n + React.memo (v11.9.77):** `_lang` é um global de módulo (`let _lang`), NÃO state do React — App o sincroniza com `profile.lang` no render (em `src/90-app.jsx`: `_lang=lang`). Consequência: componentes `React.memo` que renderizam texto **NÃO re-renderizam ao trocar o idioma** (props não mudam) e congelam no idioma anterior → bug "metade EN, metade PT". **Regra: todo `React.memo` que mostra texto DEVE receber `lang={lang}` em TODOS os call sites** (mesmo que internamente leia `_lang`) — o prop muda no switch e força o re-render. Já cobertos: Ring, CuriosityCard, TimerBar, InsightCards, SleepBlock, EntryRow, StatsPage, HistoryPage. Ao criar novo componente memoizado com texto, passar `lang`. (Pendente: overlays pré-React — girar/carregando/atualizando — são fixos pois rodam antes de `_lang` existir; exigem ler localStorage/navigator.language.)
- **ProfilePage / Ajustes (v11.9.75-76):** redesenhada pra ser coesa com o cosmos. Header com título gradiente + hero (avatar/nome/idade). Render organizado em **6 grupos** com card consistente (`linear-gradient(180deg,rgba(22,28,60,0.55),rgba(20,26,60,0.32))` + `T.gBSoft` + radius 18 + `T.insetTop`) e eyebrow colorido com ícone: **Perfil** (purple/star: nome/nascimento/meta + dados de nascimento + ver-crescimento), **Rotina** (accent/clock), **Preferências** (cyan/gear: idioma + manter-tela-ligada), **Notificações** (amber/bell: push + lembretes remédio + lembrete mamada como sub-cards com divider), **Dados** (green/cloud: eyebrow + `<BackupSection/>`), **Sobre** (pink/star: versão/changelog + streak). Toda a lógica/handlers/state ficam ACIMA do `return` e não foram tocados na reorg.
- **⚠️ Haptics são NO-OP no iOS:** `Haptic.*` (light/medium/heavy/success/warning em `js/device-features.js`) usa `navigator.vibrate()`, que o **iOS Safari/PWA NÃO implementa** (decisão da Apple). As chamadas ficam no código (funcionam no Android) mas **não fazem nada no iPhone** — o device alvo do William. Logo: NUNCA propor "feedback tátil/haptic" como recurso premium pra ele. Pra "feedback de ação/conquista", usar equivalente VISUAL (confetti, count-up, spring, flash). O toggle de vibração foi removido na v11.x exatamente por isso.
- **Bilíngue**: Português e Inglês (toda a interface, insights, curiosidades e changelog)

## Stack

- **HTML + React CDN** (ainda sem bundler, sem TypeScript, sem npm em produção)
- **React 18** via CDN (unpkg)
- **JSX pré-compilado em build-time** via `@babel/core` + `@babel/preset-react` — o navegador NÃO carrega mais `@babel/standalone`. Esse é o único passo de build. **O código do app vive em `src/*.jsx` (v11.9.85)** — o build concatena os arquivos na ordem do nome (prefixo `NN-`) e transpila como UM bloco só (escopo único compartilhado, SEM bundler/módulos/imports). O `index.html` virou só um shell com `<script type="text/babel"></script>` vazio que o build preenche. Como é concat→transpile, o compilado é idêntico ao do antigo arquivo único (provado por diff na migração).
- **Firebase 10.x compat** via CDN — Firestore para persistência
- **PWA** com manifest.json, ícones e splash screen
- **GitHub Pages + Actions** para deploy (a partir de v10.0.0)

### Regra de stack (atualizada em v10.0.0)

O princípio "sem build" foi relaxado *com justificativa de performance*: a transpilação JSX saiu do runtime do navegador pro build step, porque Babel Standalone travava o cold start em iPhone PWA. Isso **não** abre precedente pra adicionar bundler (Vite/webpack), TypeScript, ou npm em produção. O build tem 1 função só: JSX → JS. Se a próxima proposta for "adicionar X ao build", questionar se o ganho justifica — a simplicidade é o ponto.

## Arquivos do repositório

```
louise-pro/
├── index.html               ← SHELL: <head>, boot script, <style></style> e <script type="text/babel"></script> VAZIOS que o build preenche, + <script src> das libs. NUNCA servido direto. (v11.9.85 app→src/; v11.9.86 CSS→styles.css.)
├── styles.css               ← CSS do app + TODAS as @keyframes/animações (v11.9.86). O build re-inlina no <style> vazio do index.html (cold start sem request extra). EDITE O CSS AQUI. "keyframes no topo" em notas antigas = topo deste arquivo.
├── src/                     ← CÓDIGO DO APP (JSX). Build concatena na ordem do nome (NN-) + transpila como 1 bloco. EDITE AQUI, não no index.html.
│   ├── 00-core.jsx          ← APP_VERSION, CHANGELOG ref, constantes/dados, helpers (todayStr, suggestQuickActions…)
│   ├── 10-ui-base.jsx       ← Icon, T (tokens), INP_BASE, TYPES, I (i18n), L/TL
│   ├── 20-ring.jsx          ← Ring
│   ├── 22-timer-cards.jsx   ← LastFeedCard, EditStartModal, NursingSidePicker, TimerBar
│   ├── 24-widgets.jsx       ← CuriosityCard, CountUp, Confetti, InsightCards, Toast, Modal, Fld, Seg
│   ├── 30-sleepblock.jsx    ← SleepBlock, MidnightMergeCard (v11.9.140), EntryRow
│   ├── 32-addform.jsx       ← AddForm
│   ├── 34-inbox.jsx         ← InboxPanel
│   ├── 40-profile.jsx       ← ProfilePage
│   ├── 42-changelog-update.jsx ← ChangelogModal, UpdateToast
│   ├── 50-stats.jsx         ← StatsPage
│   ├── 52-history.jsx       ← HistoryPage
│   ├── 54-growth.jsx        ← GrowthPage
│   ├── 55-behavior.jsx      ← BehaviorPage (curva de comportamento, v11.9.110)
│   ├── 56-milestones.jsx    ← MILESTONE_BADGES, MilestonesPage
│   ├── 60-starfield-backup.jsx ← STARFIELD_DATA, Starfield, BACKUP_KEY, BackupSection
│   └── 90-app.jsx           ← App (componente raiz, ~1.260 linhas) + ReactDOM mount
├── manifest.json            ← config PWA (fica no root por convencao)
├── sw.js                    ← service worker do PWA (DEVE ficar no root — scope)
├── firebase-messaging-sw.js ← service worker do FCM (DEVE ficar no root — Firebase espera path fixo)
├── README.md
├── AGENTS.md
├── .gitignore
├── js/                      ← libs auxiliares (plano JS, não passam pelo build)
│   ├── changelog.js         ← histórico de versões bilíngue (window.CHANGELOG) — v11.9.84
│   ├── curiosities.js       ← curiosidades bilíngues (dia 1 → mês 12)
│   ├── milestones.js        ← marcos do desenvolvimento (CDC 2022 + WHO MGRS + SBP, 0-24m)
│   ├── routine-engine.js    ← engine de análise de padrões (sleep + feed)
│   ├── who-growth.js        ← tabelas LMS OMS + funções de percentil
│   ├── wake-lock.js         ← helper de Wake Lock API
│   └── device-features.js   ← helpers de device (haptics etc.)
├── assets/
│   └── icons/
│       ├── icon-192.png / icon-512.png / apple-touch-icon.png
├── build/                   ← tooling de build (só em dev/CI, não em prod)
│   ├── build.mjs            ← inlina styles.css + concatena src/*.jsx (JSX→JS) + bundla js/ + injeta versão no sw.js
│   ├── package.json         ← deps: @babel/core + @babel/preset-react
│   └── .gitignore           ← ignora node_modules e package-lock
├── .github/workflows/
│   └── deploy.yml           ← GitHub Action: build + deploy para Pages em todo push na main
└── dist/                    ← (gitignored) output do build, gerado pelo CI em cada deploy
```

**Regra:** NÃO mover `sw.js`, `firebase-messaging-sw.js`, ou `manifest.json` pra subpastas. Service workers têm scope baseado no path do arquivo; moveu, quebrou notificações. Firebase Messaging procura por `firebase-messaging-sw.js` no root do scope do app.

**Build step (resumo):**
- Edita: **`src/*.jsx`** (o código do app, split por componente/página) — NÃO o `index.html` (shell). **CSS/animações em `styles.css`** (re-inlined pelo build). Dados/changelog/curiosidades/marcos em `js/`. (Refs de "linha ~N" espalhadas neste doc são do monolito pré-v11.9.85; agora ache o componente pelo nome no `src/` correspondente.)
- Build: `cd build && npm install && cd .. && node build/build.mjs` — gera `dist/`
- Deploy: push na `main` → GitHub Action roda o build e publica `dist/` via `actions/deploy-pages`
- Pre-requisito **uma vez só no repo**: Settings → Pages → Source = "GitHub Actions"

## ⚠️ Hospedagem em TRANSIÇÃO: GitHub Pages → Firebase Hosting (ago/2026)

**Estado atual: os DOIS estão no ar servindo a mesma versão.** Todo push na `main` publica nos
dois — o workflow tem o step do Pages e o do Firebase Hosting.

- **Antigo (ainda ativo):** https://williamscchulz-was.github.io/louise-pro/
- **Novo:** https://louise-pro.web.app

**Por que migrar:** o William quer **privar o repositório**. No GitHub Pages, repo privado no
plano Free **despublica o site** — confirmado na doc. O Firebase Hosting separa hospedagem de
código, e roda no plano **Spark** (10 GB / 360 MB por dia; o app tem ~1,4 MB), então não conflita
com desativar o Blaze.

**Config:** `firebase.json` (raiz = `dist/`) + `.firebaserc`. Os paths do app já eram todos
relativos (`js/...`, `assets/...`, `start_url: ./index.html`), então funciona igual na raiz do
Firebase e no subpath do Pages — nada no código precisou mudar.
⚠️ `sw.js`, `firebase-messaging-sw.js`, `index.html` e `js/**` são servidos com **no-cache**:
o `app-libs.js` NÃO tem hash no nome, então cache HTTP longo serviria código velho depois de um
deploy (é a classe do bug da v10.0.1, "SW servindo HTML antigo").

**CI:** o step do Firebase é **condicional** ao secret `FIREBASE_SERVICE_ACCOUNT` existir, com o
`env` no nível do JOB (um `env` declarado dentro do próprio step não é visível no `if` dele, que
é avaliado antes). Sem o secret o CI segue verde e publica só no Pages, em vez de falhar o build.
O secret já está configurado (service account `firebase-adminsdk-fbsvc@louise-pro`, chave
`fbae6645...` de 21/ago/2026). Testado por `workflow_dispatch`: step retornou `success`.

**⚠️ O QUE FALTA (passos do William, nesta ordem):**
1. Instalar `louise-pro.web.app` nos 2 iPhones e **reconceder a permissão de notificação**.
2. **Confirmar que o lembrete de mamada volta a chegar.** Tokens FCM são vinculados à ORIGEM —
   os tokens do domínio `github.io` não valem no `web.app`, e o push fica mudo até o app
   re-registrar. É o único componente que a troca de domínio realmente quebra.
3. Só então: desativar o Pages (e remover o step do workflow) e privar o repo.

**Efeitos colaterais da troca de domínio (nenhum perde dado):** localStorage zera (flags de
"já vi a versão", backup do dia, inbox lida, splash); o cache offline do Firestore começa vazio
e re-baixa ~2.259 docs na 1ª abertura; o PWA precisa ser reinstalado (é outro app pro iOS).

**Sugestão registrada, não decidida:** apontar um **domínio próprio** (custom domain + SSL entram
no Spark) evitaria passar por essa reinstalação de novo numa futura troca de host.

## Ambiente local (multi-máquina)

O William trabalha neste repo a partir de **várias máquinas** via Codex (PC de casa, PC do trabalho, MacBook). Todas compartilham o mesmo remote (`origin/main`) como fonte da verdade.

- **Windows** (casa/trabalho): Windows 11, shell bash (Git Bash / MSYS) — sintaxe Unix (`/dev/null`, forward slashes). Raiz: `C:\Users\willi\Documents\projects\louise-pro`. Worktrees em `...\.claude\worktrees\<nome>`.
- **MacBook**: raiz `/Users/williamschulz/louise-pro`. `node`+`npm` e `gh` (autenticado) instalados em `/usr/local/bin`.
- Codex tem acesso direto ao filesystem e ao git em qualquer uma — edita no lugar, sem upload manual.

## Workflow multi-máquina — sync automático (NÃO QUEBRAR)

**Problema que isso resolve:** como o William alterna entre máquinas, um clone pode ficar muito atrás do remote. Já aconteceu de uma sessão refazer do zero uma tarefa que já estava feita e deployada (clone preso 55 commits atrás). A regra abaixo torna isso impossível de repetir.

**A fonte da verdade compartilhada é o REPO (este AGENTS.md + código versionado), nunca a memória local do Codex.** A memória local do Codex (quando existir) é por-máquina e NÃO sincroniza entre os PCs. Logo: **todo conhecimento novo do projeto (decisão arquitetural, bug latente, invariante) vai pra ESTE arquivo e é commitado** — assim viaja pra todas as máquinas. A memória local só guarda ponteiro ("ler o AGENTS.md do repo").

**Hooks de sync automático** (em `.claude/hooks/`, versionados, ativos em todas as máquinas):
- `sync-pull.sh` (SessionStart): no início de toda sessão, `git fetch` + `git pull --ff-only` se o clone estiver atrás e limpo. Se estiver atrás **e** sujo, avisa pra resolver antes de trabalhar. **Consequência prática: toda sessão começa atualizada.** Mesmo assim, antes de assumir que uma tarefa "não foi feita", checar `git log --oneline -20` — pode ter vindo de outra máquina.
- `sync-push.sh` (Stop): ao fim de cada turno, se houver commit local à frente de `origin/main` e a working tree estiver limpa, faz `git push origin HEAD:main`. **Não commita nada** (só empurra o que o Claude já decidiu commitar) e **nunca força** — push divergente falha limpo e avisa.

**Disciplina que o Claude deve manter** (os hooks são a rede de segurança, não substituem isto):
1. Começar lendo o estado: o auto-pull já rodou, mas confirmar com `git log` se a tarefa pedida não foi resolvida em outra máquina.
2. Toda entrega funcional = commit focado (com bump de versão + changelog + este AGENTS.md no mesmo commit, quando aplicável). O auto-push leva pro remote ao fim do turno.
3. Nunca deixar trabalho só local "pra depois" — se está pronto, commita (o push é automático).

**Setup dos hooks numa máquina nova:** eles vêm versionados, então um `git clone`/`git pull` já os traz. O Codex os ativa a partir da sessão seguinte (precisa que `.claude/settings.json` os referencie — também versionado). Em máquina realmente nova, abrir `/hooks` uma vez (ou reabrir o Codex) garante que o watcher carregue.

## Firebase

- **Projeto**: louise-pro
- **apiKey**: AIzaSyBPiWTlC0ZKLEX5Z8PqKM2c3zaXN8p7DH8
- **appId**: 1:670065554630:web:e8bdf489a3db28b8fe53c7
- **Sem login** — dados compartilhados entre os 2 dispositivos via Firestore

### Firestore Collections

- `entries/` — todos os eventos registrados (mamadas, **comida/introdução alimentar (`food`)**, sono, fraldas, banho, medicina, temperatura, crescimento, **`milestone`** v11.9.56)
  - `food` entries (v11.9.148) têm: `type:"food"`, `name` (alimento, ex. `Banana`), `date`, `time`, `notes` opcional e `id`. O catálogo reutilizável é derivado das próprias entries, não salvo num documento separado.
  - `milestone` entries têm: `type:"milestone"`, `key` (chave de `window.DEV_MILESTONES`), `category` (motor_gross/motor_fine/language/social_emotional/cognitive), `date`, `note` opcional. Lista pre-definida em `js/milestones.js` (85 marcos 0-24m, CDC 2022 + WHO MGRS + Denver II + AAP, ordenados por `checkupAge`) + `window.CONCERNING_SIGNS` (17 sinais "Act Early"). A engine ordena `window.DEV_MILESTONES` por `checkupAge` ao carregar — a Home usa `upcoming[0]` (ordem do array), então a ordenação importa.
  - **Namespace note:** `window.DEV_MILESTONES` é diferente de `window.MILESTONES` (`curiosities.js` — curiosidades mensais 2-12m, schema `{m,pt,en}`). Os 2 coexistem sem colidir, mas atenção pra não confundir.
- `config/profile` — perfil da Louise (nome, foto, data nascimento, meta ml, idioma, lastSeenVersion, keepScreenOn, **`routine`** v11.9.40)
  - `routine.enabled` (bool) · `routine.wakeTime` · `routine.bathTime` · `routine.bedtime` · `routine.bottlesPerDay` (int) · `routine.naps` (array de `{time}`). Opt-in: feature inativa se `routine.enabled !== true`. Default no Profile UI: wake 07:00, banho 18:00, bedtime 19:30, 4 sonecas 08:15/10:45/13:30/16:00, 7 mamadas/dia. **v11.9.88-91:** rotina ganhou 3 estágios pós-banho. **v11.9.90 (fix importante):** o default é aplicado em **RUNTIME** no `routineState` (`pbTimeStr = r.postBathBottleTime || banho+15`) — a v11.9.88 exigia salvar os Ajustes, mas o Salvar nem liberava sem mudança (dirty-check), então os estágios nunca apareciam. O único campo em Firestore é `routine.postBathBottleTime` (string "HH:MM"), que SOBRESCREVE o default. **Myrafer (era Floripa até v11.9.104) E Vit. D NÃO têm campo/config: são CALCULADOS (v11.9.91)** = mamadeira pós-banho + 5min (horário REGISTRADO da mamadeira quando ela já aconteceu — mamou 18:28 → ambos 18:33 —, senão o alvo dela; `routine.floripaTime`/`routine.vitdTime` legados são ignorados). Matching: **Myrafer/vit. D casam por NOME em qualquer hora do dia** (`medToday(re)` — remédio diário dado hoje = feito; o horário do slot só ordena o "próximo"); regexes `/myrafer/i` e `/vit(amina)?\s*\.?\s*d/i`. **Mamadeira pós-banho** casa por janela ASSIMÉTRICA `findMatch(["bottle"],tMin,45,90)` — janela curta pra trás pra não pegar a mamada pré-banho. Slots deslocam com o acordar via `adj(tMin,"pb")` (Myrafer ancorado em registro real não desloca de novo). Tipos novos (`pbbottle`/`myrafer`/`vitd`) NÃO entram no pool de consumo de soneca. Na Home, o card de remédios é **UM card dividido ao meio: Simeticona | Tylenol** (v11.9.89; entry `dual:[{l,c,n,f}...]` no array do grid de resumo, metades com divisória vertical, cores amber/red, regex `/tylenol|paraceta/i` via `totalTylenol`) — o grid de resumo segue **2×2** (Mamad. | Remédios / Sono | Fralda). **v11.9.83:** `bottlesPerDay` virou só FALLBACK — `routineState.bottlesTarget` é ADAPTATIVO (mediana de mamadas/dia das últimas ~21 datas com dado, exclui hoje; usa o config se <5 dias de histórico). E `routineState.nextBottleMl` sugere a próxima mamadeira = `(profile.mlGoal − tomado hoje) ÷ (target − feitas hoje)` arredondado a 5ml (só se `mlGoal>0`) — pré-preenche o AddForm via prop `suggestedMl` e aparece no card expandido. **v11.9.104:** `bath` virou TIPO-TIMER (start/stop → entry com `durationMin`, igual tummy/nap). TimerBar, centro do Ring e AddForm (`isTimerType`) tratam `bath` em cyan; banho NÃO dispara `bedtime-active` (só `type==="sleep"`); o discard <3min do TimerBar é só sono/soneca (banho curto é válido). `startTimer`/`stopTimer` são genéricos — bath cai no else do stop (entry `bath` + `durationMin`, sem wakeup). Live bath timer marca o estágio de banho da rotina (activeTimerMatch).
  - **Matching de slot (v11.9.80):** `findMatch` em `routineState` (App) marca um slot como "feito" por **sobreposição de intervalo**, não só hora de início. Cada evento vira `[início, início+durationMin]` e conta se intersecta a janela `[alvo-earlyTol, alvo+lateTol]` (helper `overlaps()`). Resolve soneca que começa cedo mas cobre o horário-alvo. Eventos sem duração (wake/bath) viram ponto → cai no "início na janela". Tolerâncias: wake assimétrico early 150/late 90; naps/banho/bed ±60. Timer ativo usa a mesma regra (intervalo `[início, agora]`). **Consumo de soneca (v11.9.87):** o matching de SONECA é por CONSUMO — 1 evento (ou o timer ativo, que entra como 1 token só) satisfaz no MÁXIMO 1 slot. Bug original: soneca longa (ou alvos deslocados pelo acordar) intersectava 2 janelas e o `findMatch` stateless marcava as duas como done. O pass roda DEPOIS do `slots.sort` (slots em ordem cronológica), e cada slot de soneca pega o candidato não-consumido mais PRÓXIMO do alvo (`|início−alvo|`, tie-break: início menor). Só sonecas — wake/bath/bed/pós-banho usam tipos disjuntos e ficam no `findMatch` normal. **Ajuste por acordar (v11.9.83):** `routineState` calcula `wakeDelta = acordou − alvo` (do evento `wakeup` de hoje; trava em ±120min, ignora <10min) e desloca os `targetMin` de sonecas/banho por esse delta (helper `adj()`); o **bedtime fica ANCORADO em `[bedtime, bedtime+30]`** (config 19:30 → janela 19:30-20:00). O `target` exibido vira o horário JÁ ajustado (`minToTime(tMin)`); badge "horários ajustados" aparece no card expandido quando `wakeDelta!==0`. `wake` em si não desloca (mostra o horário real via `match`).
  - **Quick buttons adaptativos (v11.9.83):** os 3 botões da Home (grid `repeat(3,1fr)`, só com `!activeTimer`) saem de `quickSuggestions` (useMemo, acima do gate de splash) → `suggestQuickActions(entries,todayE,nowMin,routine)`, função PURA top-level logo após `nowTime()`. Nota por candidato = frequência naquela hora (±90min, recency-weighted meia-vida ~14d, exclui hoje) + transição "o-que-segue-o-último-evento-de-hoje" (≤90min) + proximidade da rotina − cooldown (penaliza o que acabou de fazer). Priors por faixa de horário cobrem cold-start. Candidatos: bottle/nursing/diaper/nap/sleep/bath/medicine. O "+" (FAB) mantém acesso às 10 ações. `nowMin` é lido no render (SEM tick novo — refresca ao abrir/registrar/navegar; staleness só se ficar parado >30min, aceitável). Botões recém-surgidos animam via `.qbtn-appear` (só no mount, porque `key={tipo}` → tipo estável não remonta).
- `config/meds` — medicamentos salvos pra quick-select
- `config/active` — timer ativo (sync em tempo real entre dispositivos)
- `config/inbox` — estado da caixa de notificações
- `config/reminders/items/{id}` — lembretes. `tipo:"feedingInterval"` (default 120min desde v10.4.1) ou `tipo:"scheduled"` (medicamento com horários)
- `config/backups` — snapshot único de backup (v11.4). Fields: `snapshot`, `updatedAt`, `size`. Sempre sobrescrito pelo mais recente. Compartilhado entre os 2 devices do casal. Auto-save a cada 24h via App root. Limite 1 MiB (Firestore hard limit) — `FB.saveBackup` falha com erro claro se passar disso.

-----

## Arquitetura visual — portals, z-index, stacking

Descoberta dolorosa na v10.4.4 que vale tatuar:

- `<body>` tem dois filhos diretos: `#root` (app) e `#nav-host` (portal onde vivem `TimerBar` e a `nav` flutuante).
- Desde v10.4.4: `#nav-host` é `position:fixed inset:0 pointer-events:none z-index:300`. **Precisa ser maior que qualquer overlay** pra nav não ficar atrás.
- Mapa de z-index de overlays no app:
  - `Modal` / `UpdateToast`: 150
  - `ProfilePage` / `Sheet`: 200
  - `InboxPanel`: 210
  - `#nav-host` (nav+timer portal): **300**
  - `#load-debug`: 9998
- `#nav-host` tem `pointer-events:none` pro conteúdo atrás receber clique. Filhos que precisam clicar (a `nav`, o `TimerBar`) restauram com `pointer-events:auto` no próprio estilo.
- Position:fixed DENTRO de ancestor com `overflow:auto` vira efetivamente absolute em iOS PWA standalone (bug conhecido do WebKit). Por isso nav + TimerBar ficam fora do App root via `ReactDOM.createPortal`.
- TimerBar tem prop `hidden` que desmonta o bar quando qualquer overlay inferior abre (Sheet/Modal/ProfilePage/InboxPanel/etc) — evita cobrir botões Save e evita `backdrop-filter` pegar cor errada. Não tenta fade (o tick de 1s resetava a transition).

-----

## Linguagem visual — minimalista (atual, v10.7.0)

- Cards do Home: tint sutil com `${color}06` (3% alpha) + borda `${color}0a`. Starfield atrás. MINIMAL.
- Círculos do popup + : dark radial gradient + borda colorida sutil + soft glow externo.
- Ring: SVG puro com arcs, sem disco central. **Miolo atrás do tempo (v11.9.82):** ganhou uma nebulosa suave que respira (`.ring-neb` + `.ring-neb2`, bedtime mais roxo/intenso) + poeira estelar (`.ring-dust-a/b/c`) + halo pulsando (`.ring-pulse`, só com `activeTimer`) — preenche o vazio chapado que ficava feio atrás do número. **Regra:** essas animações são por CLASSE CSS (keyframes `ringNebBreathe`/`ringNeb2Breathe`/`ringPulseHalo`/`ringDustA/B/C` definidos no topo, logo após `body.bedtime-active`), NUNCA inline — o Ring re-renderiza no tick de 5s e o inline resetaria a anim. Tamanhos são `sz*fator` (sz = lado do ring); posições da poeira são FIXAS/determinísticas (nada de `Math.random` no render, senão pula a cada tick). Os elementos entram ANTES do div de texto central (DOM order = texto por cima) e os ícones em órbita têm `zIndex>=2` (ficam acima da nebulosa, que é `zIndex:auto`). Text center flutua sobre tudo.
- Regra geral: quando em dúvida, menos > mais. O cosmos atrás é a identidade.

### Sistema tipográfico (v11.9.39)

Escala fixa de 7 níveis em `T`. **Toda nova label/texto inline deve usar uma dessas constantes.** Magic-numbers tipo `fontSize:14` viram débito técnico — corrigir on-the-fly se topar com um:

| Constante | Valor | Uso típico |
|-----------|-------|------------|
| `T.fXS`   | 9     | Eyebrow, caption, hour marks, micro labels |
| `T.fSM`   | 11    | Labels, body small, badges, chip text |
| `T.fMD`   | 13    | Body, list primary, detail |
| `T.fLG`   | 15    | Sub-heading, card title |
| `T.fXL`   | 17    | Heading, modal title |
| `T.f2XL`  | 22    | Display small (Ring center, duration headlines) |
| `T.f3XL`  | 28    | Display (big numbers no Home/Stats) |

Hero displays raros (34, 36, 52) ficam inline — usar só pra splash, conquista, momento especial. Não criar constante pra menos de 5 usos.

Mudar a escala = mudar valor da constante em 1 lugar — reflete em todos os ~390 usos automaticamente. Por isso a escala não cresce: 7 níveis é o limite. Se quiser um valor "no meio" (ex: 14), pensar duas vezes — provavelmente um dos níveis já serve.

### Tokens de cor (v11.9.66)

Além de `accent/text/sub/dim/purple/green/...`, o `T` ganhou 3 tokens que antes eram hex soltos espalhados (~68 ocorrências consolidadas): `T.heading` (`#f0f2ff`, branco de título sobre card), `T.label` (`#7a80a8`, label de campo), `T.lilac` (`#c4b5fd`, lavanda de destaque/ativo). **Toda nova cor de texto deve usar um token** — não reintroduzir hex literal em `color:"#..."`. Hex literal AINDA é ok dentro de gradientes/box-shadow (strings planas onde não dá pra interpolar variável). Pendente (adiado pra mockup): escala de raio de borda — hoje há ~20 valores de `borderRadius` (9/10/11/12/13/14 fazem o mesmo papel); snap muda o visual, então precisa de revisão antes.

### Helper `edgeGlow()` — dormindo

O helper `edgeGlow(hex, scale, intensity)` + constantes `EDGE_GLOW_BG`/`EDGE_GLOW_BG_SOFT` continuam em index.html (logo após TYPES). Foram usados na v10.6.x (Beautiful Shadow style — 4 inner shadows em camadas) e REVERTIDOS na v10.7.0 por sobrecarregar visualmente. Deixados como ferramenta caso queira aplicar em algo isolado no futuro (app icon, conquista, etc). Não usar em vários elementos simultâneos.
- As 4 inner shadows embutidas simulam as da referência Figma (Y:-80 Blur:60 primary glow + Y:-40 halo + Y:-20 white kiss + Y:6 top hint). GPU-cheap em iOS — diferente do backdrop-filter blur, este efeito é barato em listas longas.
- NÃO aplicado no app icon (launcher) por pedido do William.
- Se criar novo componente com glow: use sempre o helper, não copie as shadows inline. Consistência.

-----

## Performance iPhone PWA — regras aprendidas

Bugs de perf descobertos e corrigidos durante as auditorias. Valem tatuar:

- **App-level tick interval**: durante timer ativo, 5s (não 1s). O arco do Ring é imperceptível se mover a cada 5s (<2° por passo em nap de 20min). Usar 1s faz o App inteiro re-renderizar 9x3600 vezes numa noite de bedtime — bateria derrete. Exceção: o próprio TimerBar tem tick interno de 1s pra display "17:03", fine.
- **CSS animations/transitions: SEMPRE via classe CSS, NUNCA inline.** Componentes com re-render frequente (qualquer coisa que recebe `tick` como prop) vão resetar a animação se ela estiver no inline `style.animation`. Primeiro caso: TimerBar v10.3.2 (fade). Segundo caso: Ring disc breathe v10.6.2. Regra: toggle a classe via `className={cond?'foo':''}` e defina `@keyframes` + `.foo{animation:...}` no CSS estático lá em cima. Inline style reset a cada re-render, classes não.
- **React.memo em componentes pesados**: Ring, SleepBlock, EntryRow têm memo desde v10.5.1. Sem memo, qualquer re-render do App (mudança de qualquer state) força redraw mesmo quando props são idênticos. Regra: componente visualmente pesado + props estáveis → embrulhar em React.memo.
- **backdrop-filter é QUADRÁTICO no raio**: blur(26px) não custa 2x blur(13px), custa ~4x. Em elementos sempre visíveis (nav pill), usar no máximo blur(18-20px) com saturate(180%). Evitar empilhar mais de 2 blurs visíveis ao mesmo tempo.
- **body.app-hidden pausa animações**: `body.app-hidden *{animation-play-state:paused !important}` implementado no CSS. PWA em background não gasta CPU com mercury ring spinning, twinkle, pulse. NÃO remover.
- **Firestore offline persistence ON desde v10.5.2**: `db.enablePersistence({synchronizeTabs: true})` chamado logo após `firebase.firestore()`. O SDK cacheia tudo no IndexedDB — cold start PWA é instantâneo (serve cache + sync em background). App também funciona offline. NÃO remover esse call. `synchronizeTabs: true` é essencial pra não dar lock conflict quando Safari + PWA standalone abertos simultaneamente.
- **Firestore subEntries com janela de 90d desde v11.9.0**: `FB.subEntries(cb, daysBack=90)` aplica `where('date','>=',cutoff)` automaticamente. Cutoff é calculado no setup (estático até o app reabrir; entries de hoje continuam aparecendo pq today >= cutoff). Reduziu reads em ~80% pra usuários com >6 meses de uso. **NÃO existe "load older"** — `loadOlderEntries` foi removido na v11.9.34 como dead code (nunca foi conectado na UI). Se um dia precisar ver >90d no History, re-adicionar é trivial: nova função `loadOlderEntries(beforeDate)` + state slice separada + botão "Carregar mais" no fim do scroll do History.

-----

## State per-device vs shared — regra (v11.1)

O app é usado pelo William + esposa em 2 iPhones simultâneos, com Firestore compartilhado. Regra de divisão do state:

- **Compartilhado (Firestore)**: dados do bebê (entries, profile básico, timer ativo, config de reminders), notificações GERADAS pela engine (inbox.items).
- **Per-device (localStorage)**: "eu já vi essa versão" (`lp_last_seen_version`), "eu marquei essa notificação como lida" (`lp_inbox_read` — Set de keys). A engine continua gerando a mesma notificação pros 2 devices, mas cada um rastreia sozinho quais marcou como vistas.

Regra prática: se o state é "eu, neste device, já percebi X", vai pra localStorage. Se é dado da Louise ou config compartilhada, Firestore.

- Legacy `inbox.items[].read === true` do Firestore AINDA é respeitado como fallback (OR com localReadKeys) pra não quebrar dados pré-v11.1.
- `profile.lastSeenVersion` do Firestore é usado APENAS como fallback na primeira abertura pós-v11.1 (se localStorage vazio MAS Firestore tem valor, herda pra não spammar toast).

-----

## Integridade de dados — NUNCA QUEBRAR

Perda de dados aconteceu na v10.4.7 (foto + nome da Louise sumiram). Fix em v10.5.0. Regras absolutas daqui pra frente:

- **Todo `.set()` no Firestore precisa de `{merge:true}`** salvo quando explicitamente fizer sentido substituir o doc inteiro (ex: `saveTimer` ou entries novos). Atualmente com merge: `saveProfile`, `saveInbox`. Sem merge: `saveTimer` (state completo do timer), `addEntry` (entry novo), `saveMeds` (sempre full list), `savePushToken` (doc dedicado por token), `addReminder` (reminder individual).
- **Side-paths NUNCA mandam spread de estado em memória**. Se o objetivo é marcar um campo, manda só `{campo: valor}`. Exemplos:
  - `markChangelogSeen` → `FB.saveProfile({lastSeenVersion:APP_VERSION})`, não `{...profile,lastSeenVersion}`.
  - `persistToggle` no Profile → manda só o override, não o patch completo.
- **Causa do bug original**: race condition (onSnapshot ainda não tinha disparado) + multi-aba + `.set()` replace + spread de `profile` default em memória. Qualquer combinação dos 3 fatores e os dados sumiam.
- Se precisar deletar um campo explicitamente, usar `firebase.firestore.FieldValue.delete()` (merge ignora `undefined`).

-----

## Lista do Hoje (Home) — regras de filtro

Filtro complexo que já gerou vários bugs. Estado atual (v10.4.6+):

- `todayE = entries.filter(e => e.date === today)`.
- Um evento é **escondido** da lista apenas se:
  1. `e.nightWake && liveBedtime` e o tempo cai dentro do liveBedtime (evento será mostrado dentro do SleepBlock live), OU
  2. `findContainingBedtime(e)` retorna um bedtime que **vai realmente ser renderizado** como SleepBlock (live OU `cb.date===today && cb.wakings.length>0`). Caso contrário, mostra como EntryRow normal (evita evento órfão).
- `findContainingBedtime` usa **bordas estritas**: `evMs > startMs && evMs < endMs`. Evento exatamente na hora de início/fim conta como fora — caso típico: primeira mamada da manhã logada na mesma hora que o bedtime terminou. Mesma estritude aplicada em `retroactiveEvents` do SleepBlock e `linkedEvents` das wakings.
- Spacer do bottom do scroll do Home: `calc(80px + safe-area)` sem timer ativo, `calc(160px + safe-area)` com timer (nav pill em z:300 tapa os últimos pixels se o spacer for menor).

-----

## Sistema multiagente de engenharia (v11.9.138, sem bump de versão)

Protocolo permanente de como este projeto executa tarefa não-trivial — formalizado a pedido do William em 30/jul/2026. A hierarquia de modelos (subseção abaixo) já seguia o espírito disso; esta seção é o processo em volta dela.

**Papéis:** Sonnet 5 (o loop principal) é o **OPERADOR** — arquiteta, executa o que não delega, e é o responsável final por tudo que é entregue, inclusive o que veio de subagente. Haiku e Sonnet também atuam como **executores delegados** via `Agent` (subagentes). Opus e Fable são **consultores sob demanda**, nunca executores de rotina — a escada linear Sonnet→Opus→Fable da subseção abaixo continua valendo tal como está.

**Classificação de tarefa** (antes de executar):
- **Classe A (mecânica)** → `model:"haiku"`: busca em massa, boilerplate, renomeação, formatação, coleta repetitiva.
- **Classe B (convencional bem especificada)** → subagente `model:"sonnet"` quando compensa paralelizar ou isolar contexto (telas, CRUD, testes, refatoração delimitada); senão, o Operador mesmo executa.
- **Classes C e D** (arquitetura, segurança, dado sensível, migração, bug difícil, integração crítica) → o Operador executa direto, com atenção redobrada; consulta o conselheiro (Opus — que por sua vez decide se aciona o Fable) só em impasse real.

**Delegação:** toda tarefa delegada leva escopo fechado, proibições explícitas e critério de aceitação verificável. Todo retorno de subagente é revisado pelo Operador antes de integrar — nada entra no código sem essa revisão.

**Fluxo por tarefa relevante:** entender o pedido (perguntar se ambíguo, não inventar) → inspecionar o repo/estado real → planejar → **revisão adversarial do próprio plano** (procurar ativamente por que falharia; declarar PLANO APROVADO / COM AJUSTES / REFORMULADO) → delegar (Classe A/B) ou executar (B/C/D) → revisar, integrar, testar → **auditoria independente antes de entregar** (procurando motivo pra REJEITAR — pode ser um subagente auditor) → corrigir o que a auditoria achou → entregar com status explícito: APROVADO / COM RESSALVAS / BLOQUEADO. Proporcional à tarefa — um typo não pede os 9 passos; uma migração de dado ou bug de arquitetura pede.

**Escalação além do Operador:** quando os critérios batem (2 abordagens já falharam, causa raiz desconhecida, risco de perda de dado, operação ampla multi-arquivo, segurança sensível sem conselheiro disponível), o Operador para com segurança e pede explicitamente ao William: **"AÇÃO NECESSÁRIA — ALTERAR O LOOP PARA OPUS/FABLE"**, com um prompt de continuação autocontido pra retomar depois da troca. Isso é diferente de escalar uma SUBTAREFA pro Opus via `Agent` (caminho normal do dia a dia, ver subseção abaixo) — é pedir a troca do modelo do LOOP PRINCIPAL em si, último recurso. Nunca fingir que trocou de nível/modelo sem essa troca real ter acontecido.

**Orquestração pesada (`Workflow`/Ultra):** só com opt-in explícito do William — já reforçado pela própria ferramenta (exige a palavra "ultracode", sessão com o flag ligado, ou pedido direto nas palavras dele).

**Escopo:** melhoria fora do pedido vira tarefa anotada à parte, não implementada junto — exceto bloqueante de segurança diretamente relacionado, que pode ser corrigido na hora.

**Comunicação:** resposta direta começando pelo resultado; explicar o PORQUÊ de decisão técnica; reportar falha com honestidade e evidência (teste falhou = dizer que falhou).

### Hierarquia de esforço (adaptado pro Codex)

O Codex CLI não tem múltiplos modelos disponíveis a troco de nada como o Claude Code tinha (Sonnet/Opus/Haiku no plano Max). Aqui a "hierarquia" é de **esforço mental por tarefa**, não de modelo:

- **Rotina** (executar/edit/rodar comando, bug simples, boilerplate): resposta direta, mínimo de exploração antes de agir.
- **Substantivo** (decisão de arquitetura, bug resistente, migração, mudança que toca dado real): parar, ler o código, planejar, revisar o próprio plano procurando por que falharia, então executar.
- **Difícil de verdade** (raciocínio realmente cabeludo, algoritmo estatístico, análise de segurança profunda): é hora de pedir opinião externa antes de commitar — ex: mostrar o plano ao William e perguntar se ele quer levar pro ChatGPT com um modelo mais forte, ou usar `/model` do Codex CLI pra rodar o próximo turno num modelo maior. Não fingir que resolveu sozinho quando não resolveu.

**Legado**: `.codex/agents/conselheiro-fable.toml` existe como referência do subagente Fable (protocolo herdado da era Claude). Codex não invoca subagentes do mesmo jeito — se você quiser esse tipo de opinião de "advisor difícil", peça ao William que abra um ChatGPT em paralelo com o problema.

Regra anti-falha (independente de modelo): **nunca insistir sozinho num problema que resistiu a 2 tentativas** — reportar honestamente ao William que ficou preso, com evidência do que tentou.

### Ponytail — escada de minimalismo (adaptada do princípio, não o plugin real)

Antes de gerar qualquer código, rodar esta escada de 7 degraus, nessa ordem exata (baseado em github.com/DietrichGebert/ponytail — aqui é só o princípio nas instruções, sem hooks/plugin de terceiro rodando automaticamente a cada turno):

1. **Precisa existir?** (YAGNI — pular a feature se não for necessária)
2. **Já existe no código?** (reusar antes de reescrever — ex: `T.iconTile`, `T.cardShadow`, `T.pageBg` já existem exatamente pra isso)
3. **Standard library resolve?**
4. **Recurso nativo da plataforma resolve?** (ex: `<input type="date">` em vez de um date-picker customizado)
5. **Dependência já instalada resolve?**
6. **Dá pra fazer em 1 linha?**
7. **Só então: o mínimo que funciona.**

Isso formaliza o que o projeto já pratica (regra de stack: "questionar se o próximo bundler/dependência vale o ganho" — ver seção Stack). **Nunca corta em**: validação de input, tratamento de perda de dado, segurança, acessibilidade — essas guardas não entram na escada, são sempre mantidas.

Enforcement em 2 pontos: (1) **na entrada** — quem vai implementar (Sonnet, Opus quando escalado, ou Fable quando convocado) roda a escada antes de escrever ("isso precisa mesmo existir?"); (2) **na saída** — ao revisar output de um subagente delegado, rejeitar over-engineering antes de aceitar (arquivo/dependência desnecessária, abstração prematura, componente novo onde um degrau menor resolvia).

**Atalho deliberado**: se cortar caminho conscientemente (ex: hardcode temporário em vez de generalizar agora), marcar com comentário `ponytail: <o quê + caminho de upgrade>` no código — não é atalho negligente, é decisão registrada pra achar depois (`grep -rn "ponytail:" src/` levanta a lista).

-----

## Princípios e práticas (NÃO NEGOCIÁVEIS)

### Workflow obrigatório

1. **Preview-before-apply é OPT-IN (v11.9.3):** Codex segue direto com mudanças solicitadas pelo William. **Só pede autorização** quando: (a) mudança envolve risco real de data-loss/regressão; (b) escopo do pedido é ambíguo e tem 2+ caminhos divergentes; (c) o user explicitamente pede mockup primeiro (ou regra do item 2 dispara). Mockup HTML continua obrigatório pra mudanças visuais novas (item 2). Default é "manda ver".
2. **Mockup HTML primeiro** sempre que houver mudança visual ou de UI — William revisa e aprova antes de implementar. Salvar em `.claude/mockups/<nome>.html` e publicar como **Artifact** pra revisão.
   - **⚠️ REGRA DURA DE PRIVACIDADE (v11.9.143, achada em auditoria):** mockup **NUNCA** contém dado de produção, e **NUNCA** vai pro domínio público. Até a v11.9.142 o `build.mjs` copiava `.claude/mockups/` INTEIRO pra `dist/preview/` (feature da v11.9.61) — e o `relatorio-30d.html`, gerado com dados REAIS via Firestore REST, ficou servido publicamente com HTTP 200 por meses: nome completo da Louise, data de nascimento exata e 30 dias de rotina de sono/alimentação, indexável por buscador. A cópia foi **REMOVIDA do build** (não filtrada — removida): a v11.9.67 já documentava que o `/preview/` nunca funcionou como superfície de revisão pra ele, então era risco permanente de privacidade em troca de zero utilidade. Quando o mockup precisar parecer real, usar dado **sintético** ou datas relativas (D-1, D-2), sem nome e sem data de nascimento. Pra revisão no iPhone: **Artifact** (privado por padrão, link HTTPS que abre no device).
   - Corolário: ao escrever qualquer relatório de auditoria/segurança, **não** salvar em `.claude/mockups/` — publicar como Artifact a partir do scratchpad. Um relatório de vulnerabilidades no domínio público é um roadmap de ataque.
   - **⚠️ Realidade de delivery (v11.9.67):** o William NÃO consegue ver mockup por URL no iPhone de forma confiável — localhost nunca funciona, e `/preview/<nome>.html` (apesar de tecnicamente live, HTTP 200) não chega até ele na prática (provável atrito de PWA/SW standalone + URL longa pra digitar). **A superfície de revisão confiável dele é o APP LIVE deployado** — ele consegue ver versões publicadas normalmente. Então, pra mudança visual: ou (a) descrever em texto + mandar direto pro app live (reversível em 1 commit) e ele fala "mantém/reverte", ou (b) só mockar se ele pedir explicitamente. Não assumir que ele viu um mockup só porque o link existe.
3. **Commits incrementais e focados** — evitar bundling de mudanças não relacionadas
4. **Bump de versão + changelog** acompanha toda mudança funcional (no formato bilíngue novo)
5. **Atualizar AGENTS.md** no mesmo commit sempre que a mudança tiver implicação arquitetônica, descoberta de bug latente, ou alteração de invariante. A versão atual aqui em cima sempre tem que bater com `APP_VERSION` do index.html.
6. **Validação antes de entregar**: `node build/build.mjs` roda o babel transform — se der erro de sintaxe, aparece aqui. Sempre rodar antes de commitar uma mudança no index.html.

### Padrões Babel-safe (evitar travas no Babel Standalone)

- Arrow functions e template literals (não usar `function()` declarations dentro de JSX nem string concatenation)
- Sem IIFEs como argumentos de função
- Sem emoji dentro de tags `<script>`
- Verificar braces/parens/brackets balanceados antes de qualquer entrega

### Timezone (BRT, UTC-3)

- **Sempre** usar métodos locais: `getFullYear()`, `getMonth()`, `getDate()`
- **NUNCA** usar `toISOString().slice(0,10)` pra exibir datas — causa rollover às ~21h BRT
- Funções afetadas históricas: `todayStr()`, `dateOffset()`, qualquer comparação de data

### Disciplina de escopo

- Variáveis usadas dentro de callbacks devem estar definidas no escopo correto
- Verificar shadowing entre helpers globais e variáveis locais (já aconteceu com `realSleepMin`)
- Usar `e.stopPropagation()` em botões dentro de elementos clickable pra evitar bubbling
- **⚠️ LANDMINE de Rules of Hooks (v11.9.68):** o componente `App` (em **`src/90-app.jsx`**) tem um early-return de splash/loading (`if(splash||loading)return(...)`). **TODO hook (`useState/useEffect/useMemo/useCallback/useRef`) tem que ficar ACIMA desse return.** Adicionar um hook depois dele = o hook é pulado no render de splash e roda no render normal → contagem de hooks muda → **React #310 (crash "Oops! Algo quebrou")**. Foi exatamente isso que a v11.9.64 quebrou (um `useCallback` colocado logo antes do `goTo`, que vive *depois* do gate). `goTo`/`goGrowth` e afins: a função pode ficar onde quiser, mas se for `useCallback`, sobe pra cima do early-return. Mesma regra vale pra `Confetti` e qualquer componente com `return null` condicional: hook antes do return, sempre. **Validação:** `node build/build.mjs` NÃO pega isso (é erro de runtime, não de sintaxe) — só um smoke test no app pega. Cuidado redobrado ao mexer em hooks.

### Comunicação

- Voice-to-text — interpretar contextualmente, não literalmente
- Feedback via opção (ex: "Opção B", "alternativa enxuta") — implementar exatamente o que foi selecionado
- Idioma de trabalho: português (interno) com app bilíngue (PT/EN)

-----

## Workflow de entrega

```
Pedido → Mockup HTML → Aprovação do William → Implementação no filesystem
       → Bump versão + entrada bilíngue no CHANGELOG + atualiza AGENTS.md se houver mudança arquitetônica
       → Validação local (node build/build.mjs)
       → git add / commit com mensagem descritiva (AGENTS.md no MESMO commit)
       → git push → GitHub Action roda build + deploy via Pages
```

Regras de git:
- Commit apenas quando o William pedir explicitamente (ou quando o escopo da tarefa incluir "commita e pusha")
- **Sempre pra `main`**. Mesmo trabalhando numa worktree `claude/<nome>`, o push é `git push origin HEAD:main` — nada de PR intermediário, nada de deixar mudança parada numa branch. A main é a fonte da verdade e é o que a GitHub Action publica.
- Depois de pushar pro remote/main a partir de uma worktree: o working tree da main local (`C:\Users\willi\Documents\projects\louise-pro`) fica desatualizado. Pra uma nova sessão lá começar com o AGENTS.md atual, rodar `git pull` na main local (ou abrir a sessão no worktree ativo).
- Nunca force-push em `main`; nunca `--no-verify` sem pedido explícito
- Mensagem de commit curta e focada no porquê; uma mudança funcional = um commit

### Padrão do CHANGELOG bilíngue

Cada entrada é `{v, date, pt:{title, bullets}, en:{title, bullets}}`. Bullets suportam:
- `★ ` no início → highlight verde
- `**texto**` → destaque lavanda

**v11.9.84:** o array `CHANGELOG` (211+ entradas, ~4.600 linhas de dado puro) saiu do `index.html` pra **`js/changelog.js`** (`window.CHANGELOG`, bundlado em `app-libs.js` igual curiosities/milestones). Isso derrubou o `index.html` de ~10.5k → ~5.9k linhas. **Regra nova: nova versão = adicionar a entrada no TOPO do array em `js/changelog.js`** (não mais no index.html). O `index.html` só faz `const CHANGELOG = window.CHANGELOG || [];`. O histórico completo aparece no modal "Ler novidades".

## Ambiente de validação (local)

- Pasta sugerida: `C:\Users\willi\Documents\projects\louise-pro\.validate\` (ou `~/validate`) com `@babel/core`, `@babel/preset-env` e `@babel/preset-react` instalados via `npm install` local (fora do repo — `.validate/` fica no `.gitignore`)
- Validação padrão:
  1. Contagem de braces/parens/brackets via script Python curto
  2. `babel.transformSync()` via Node, simulando a ordem em que os scripts são carregados no `index.html`
- Mock mínimo antes de carregar os módulos extraídos: `global.window = {}; global.navigator = {};`
- Se o ambiente de validação não existir ainda, dizer isso explicitamente em vez de pular a etapa silenciosamente.

-----

## Próximos passos possíveis (ideias arquivadas)

- **Modo noturno do Ring** (ideia tipo Napper: zerar ring quando entra no Bedtime, mostrar meia-lua noturna com eventos da madrugada) — **arquivado por decisão do William**
- Importar dados do Napper se um dia exportarem
- Análise comparativa pai vs mãe (quem registrou o quê)
- Integração com Apple Health
- Backup automático em outro destino além do Firestore

## Coisas que NÃO devem acontecer

- Mexer no `routine-engine.js` sem motivo forte (ele é estável e mudanças nele têm alto risco de regressão)
- Adicionar dependências npm ao runtime do app (browser). npm em `build/` é ok porque roda só em CI. Não adicionar TypeScript, bundler, ou preset-env ao build sem justificativa forte de performance.
- Mudar a estrutura de dados do Firestore sem migration plan
- Quebrar backward compatibility com sleeps antigos (sem `wakings[]`)
- Adicionar features que exigem login ou autenticação
- Force-push em `main`, commits sem pedido, ou `git add -A` cego (pode pegar arquivos sensíveis)
